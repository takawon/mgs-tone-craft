# Changelog

このファイルは、利用者から見える変更の通し履歴です。
バイナリ（GitHub Releases の ZIP）を更新するときは、次を両方更新します。

1. 本ファイル（`CHANGELOG.md`）へ版ごとの項目を追記する
2. 同じ内容の要約を、当該版の [GitHub Release notes](https://github.com/takawon/mgs-tone-craft/releases) へ載せる

形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に近い書き方とします。
版番号は公開用`VERSION`（およびローカル仕様書の文書版）と揃えます。

カテゴリの目安:

- **Added**: 新しい機能
- **Changed**: 既存の振る舞いの変更
- **Fixed**: 不具合修正
- **Removed**: 削除した機能
- **Notes**: 互換性・Alpha制限など、利用者への注意

## [Unreleased]

### Added

- 総合音色エディタ右上に、SCC／OPLL音色エディタと同じスピーカーアイコンの1秒プレビューON／OFFトグルを追加
- 総合は`スペアナ`／`SCC音色`／`OPLL音色`の右、SCC／OPLLは`スペアナ`と相手側エディタ入口の右に1秒プレビュートグルを配置し、遷移ボタンを同じ寸法へ統一
- 総合／SCC／OPLLから共有するリアルタイムスペクトログラム。音源別表示トグル、時間／周波数、スクロール、倍音ガイド、アプリ内前面固定、0～100%の強調率バー＋数値入力に対応
- OPLL単音色エディタのMOD／CAR実効EGへ、AR・DR/SL・独立RRの赤い3操作点を持つエンベロープ補助表示
- `@e` 定義へ総合名・チャンネル・度数の行コメント（長い定義では1行目 `{ ,,` の直後）
- 総合音量ダブルクリックに「詳細」スイッチ（自動変化の隣）。ONだと途中コマンドがあっても無分割ランプと同じ音量段。総合形式17
- 総合音色エディタへ開く／保存／貼り付け／コピー／Undo／Redo（編集中プログラム全体の単エントリ`.mgstc`と同一クリップボード形式）
- マスターボリュームつまみの下に「音量」ラベル
- 総合初期設定の発音前 MML：`@e`仮番号、`k`（PSG／SCC）、`p`（PSG／SCC、LFOと排他）、`so`／`sf`（OPLL）。`v`は音色番号の右へ。総合形式16
- 総合音量ステップの「自動変化」編集。MGSC `f=<n>` と MGSDRV `2n cc` の整数ランプへ直列化

### Changed

- スペクトログラムは音源別強度をキャッシュし、音源ON／OFFと強調率変更ではFFTを再計算せず表示だけを再合成。無音列、変更列の再描画、周波数座標変換を軽量化し、総合音色では追加済みレイヤーに含まれる音源だけを解析
- OPLLのEG編集をMOD／CAR・EG0／EG1で同じ操作へ統一。AR点は横でARのみ、2点目は横でDR・縦でSL、右側のRR点は縦でRRのみを変更。キーオフと終端は固定の派生表示とし、グラフのホイールと赤丸以外のドラッグは編集しない
- OPLLのDR=0は2点目をAR点の右真横へ、DR=15は同じXのSL目標へ配置。AR操作でDR値を保ったまま2点目が追従。SL目標と操作中の戻り先を表示し、重複点はホバー時に分離。Shift微調整、Esc取消、全高でのRR操作、作用しない理由の表示に対応。AR=0は灰色補助線とOFFを表示し、編集は継続可能
- ASIO出力の48kHz時に内部レート変換をバイパスしてコールバック単位で直接レンダーし、非48kHz時の内部変換チャンクを64フレームへ短縮。ASIOドライバーのバッファ長設定は変更しない
- OPLL実効EGは半透明塗りを維持し、編集用の模式図と区別。MOD・EG0のキーオン中のRR減衰、DR=0かつSL=0の遷移、MODキーオフ後のAM変動を実効表示へ反映。操作確定では履歴のみを記録し、同じ値で試聴を再実行しない
- OPLL単音色EGグラフを従来の2倍の高さに拡大。キーオフ表示・実効EGシミュレーションを中央の2秒地点へ移動し、`A / AR`等の区間見出しを廃止
- `@e` の MGSC 出力: 連続同一音量を `f:2` 形式へ結合し、定義終端の `f:<n>` を `f` にする。1行255文字を超えたら改行。コンパイル上限は256バイト
- スイッチ（`SwitchLookAndFeel`）を即時発音トグルと同じON／OFF塗り・文字の半円ピル表示へ
- 総合編集段の音量レーンを約265px（100%）へ。静かな音量の段は表示を丸めて0–15を1単位で選べる。音程レーンの高さは維持
- 音量レーンの編集座標を 0～15 等間隔へ変更し、チップ固有の論理音量カーブを半透明で重ねて表示
- 複合音色の保存形式を16へ更新。既存データの自動変化はオフとして読み込む
- 総合音色画面の余白を右側の音色ライブラリと同じ密度へ。初期設定列・時間軸ヘッダ・編集ドック・主フレームは`panelPad`／`sm`／`controlGap`／`fieldH`（`UiLayout`）。0.235追加コントロールの見切れを解消
- 自動変化ONの音量棒を、水平バー＋斜め点線から開始音量～終端音量のライン棒へ変更。半透明の実効音量バーはMGSDRVのランプステップに同期する

### Fixed

- 総合音色エディタの無音中も60Hzでタイムライン全面再描画と同一MMLプレビュー更新・診断追記が続く問題を修正。波形履歴が黒へ消えた後は不要な再描画を行わない
- OPLL ADSR補助ラインで、DR=0のDecay/Sustain点をクリックするとDR=15へ変化する問題、重複点でARを選べない問題、縦方向RR点がカーソルへ追従しない問題、CAR・EG1のSLが範囲外ドラッグで反転する問題、移動なしのクリックでも編集通知と試聴が走る問題を修正
- 総合エンベロープの連続する自動変化が2本目以降`=`にならず保持になる問題を修正
- 自動変化の途中ステップへ入れた`\`／`@`／`y`が2本目以降の`f=<n>`で原点音量を落とす問題を修正。出力は命令位置の補間音量で`=`を分割し（`f.8=5.\20.0=5`）、編集画面の自動変化は分割しない
- 命令後に補間音量を1カウント原点として付けると余分なステップになり音量がずれる問題を修正（`d.8=2` → `8=2`）。1カウント区間はMGSCが拒否する`=1`ではなく16進1文字へ
- OPLLの`@e` `\` が9bit F-Numberだけを加減し、大きな値でMGSDRVとオクターブがずれる問題を修正
- 試聴が音程を別レーンで進めて`\`がMGSDRVより1カウント早く掛かる問題を修正（`\`を音量`@e`ストリームへ載せる）
- MGSDRVの音量ランプをZ80の8bit加算・剰余分配と一致させ、255付近の長い`f=<n>`で桁上がり後の変化時刻がずれる問題を修正
- MGSC 1.11が `f.0=240` を `Invalid parameter` にするため、自動変化duration 240をMGSCへ出さない
- 音量の「自動変化」を一度ONにするとスイッチでOFFに戻せない問題を修正
- 自動変化のMGSC出力が先頭音量を省略して `8=10` だけになり、`f.8=10` と一致しない問題を修正

## [0.224] - 2026-08-16

`0.215` 以降の公開 Alpha 向けまとめ（GitHub Releases 更新用）。

### Added

- **画面の大きさ（View）**: アプリ設定に 125%／100%／75% を追加。編集画面・ダイアログ全体の表示倍率（`Ctrl`± で一時上書き、`Ctrl`+`0` で View 既定へ）
- SCC 波形グラフのホバー値ポップアップ（`NN : D (0xHH)`。ポインター位置の Y に追従）

### Notes

- 引き続き Alpha。複合エンベロープの一部や `.MUS` フル出力は未保証
- 保存形式・設定 INI に後方互換の保証はない

## [0.215] - 2026-08-15

`0.165` 以降の公開 Alpha 向けまとめ（GitHub Releases 更新用）。

### Added

- **総合画面のソフトウェアエンベロープ編集**を大幅に拡充（チャンネル三段表示、共通カウントグリッド、音量／音程／`@`／`\`／休符、OPLL の TL／FB 自動など）。MGSC `@e` プレビュー（定義＋発音前トラック MML）
- 複合音色の初期設定（休符・相対音程・音色番号／OPLL ROM `@0`～`@14`、テンポ、マイクロデチューンなど）とライブラリ保存形式の更新
- 総合から割当単音色を開き、**総合出音を聴きながら** SCC／OPLL を編集する流れ
- エンベロープ互換の公開ガイド（`docs/ENVELOPE_COMPAT.md`）と回帰用マニフェスト
- ハング検知ログに処理名・経過時間・段階（検知／未回復／回復）を記録



### Changed

- 総合エディタのレイアウト（右ライブラリ、初期 1600×1050、ウィンドウをモニタ作業領域内へ収める）
- 鍵盤反応と WASAPI 低遅延寄りの初期化、画面切替／未保存確認の応答改善



### Fixed

- 操作していないときに数秒フリーズすることがある問題（PC 音声デバイス異常からの自動復帰を別スレッドへ）
- 応答しない音声デバイスがあると終了や出力切替が止まる問題（停止待ちの有限化）
- タイトルバーでの画面切替・フォーカス／ショートカット周り
- 未保存確認ダイアログが表示されないままになることがある問題
- ウィンドウがタスクバーに食い込む問題（外接矩形での作業領域クランプ）



### Notes

- 引き続き Alpha。複合エンベロープの一部操作（カウント命令の完全直列化、`[` 前後配置、`.MUS` フル出力など）は未完成・未保証
- 総合／ライブラリの保存形式に後方互換の保証はない（形式番号が進んでいます）



## [0.165] - 2026-08-11

`0.101` 以降の公開 Alpha 向けまとめ（GitHub Releases 更新用）。

### Added

- **ライブラリ管理**ウィンドウ（音色一覧／タグ管理、SCC・OPLL・複合、複製・削除・タグ付与・読込して編集、★・ソート・検索）
- 右詳細での名前・メモ編集、付与タグのチップ＋×で外す操作
- 78種の標準タグ／独自タグ、複数AND絞り込み、独自タグの一括改名・統合・削除
- お気に入り・最近使用・並べ替え、選択時試聴と A/B 比較
- 未保存変更の終了／補助画面クローズ確認
- ASIO 出力（選択・設定画面・失敗時復帰）とレート変換
- OPLL 変換の品質選択（標準／じっくり）、進捗表示、キャンセル
- MGSC プレビュー／コピー／`.txt` への音色名コメント
- Windows アプリアイコン、About の著作権／GitHub／X リンク
- ハング検知ログ（`%LOCALAPPDATA%\MgsToneCraft\hang-*.log`）



### Changed

- 鍵盤 `C1`～`B8` を MGSDRV／MGSC の `o1c`～`o8b` 対応へ統一
- UI フォント・配色・ホバー／選択ハイライトの整理（緑アクセント）
- SCC／OPLL エディタ初期サイズとライブラリパネル配置の見直し
- 埋め込みライブラリの操作配置（読込・削除・名前変更など）。複製はライブラリ管理側



### Fixed

- SCC 波形ドラッグ中の PC キー無反応
- ASIO 設定後に WASAPI へ誤って戻る問題
- タイトルバー活性化後の `Ctrl+C`／`V`／`Z`／`Y`
- A/B 表示の文字化け
- OPLL キーオフ後のハング対策（強制消音秒数）



### Notes

- 引き続き Alpha。総合画面の複合エンベロープや曲用フル `.MUS` は未保証
- ライブラリ保存形式に後方互換の保証はない（旧形式は読めない場合あり）



## [0.101] - 2026-08-09

初回の公開 Alpha。

### Added

- SCC／OPLL 単音色エディタ（波形・パラメーター編集、ライブラリ、`@s`／`@v` 入出力）
- 画面鍵盤／PCキー／MIDI／内蔵エミュレータによる試聴
- MAmi-VSIF dongle／MAmidiMEmo 経由の実機発音
- WAV／Audacity からの近似変換、SCC↔OPLL 相互近似変換
- Windows x64 配布 ZIP（`mgstc.exe` とライセンス一式）



### Notes

- Alpha（機能限定）。総合画面の複合エンベロープや曲用フル `.MUS` 出力は未保証
- 音色ライブラリの保存形式に後方互換の保証はない
