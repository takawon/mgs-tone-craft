# MGS Tone Craft 仕様書

- 文書版: 0.58
- 基準日: 2026-07-28
- 状態: 初期要求仕様（現時点の合意事項）

## 1. 文書の目的

本書は、SCC・SSG（YM2149）・OPLL（YM2413）を組み合わせたMGSDRV向け複合音色エディタについて、現時点で確定している要求と技術方針を固定する。

本書に記載された「確定事項」を今後の設計・実装・テストの基準とする。後から変更する場合は、本書の文書版と変更履歴を更新する。

## 2. 製品の目的

単一音源のパラメーター編集だけでなく、複数チャンネルを同時に鳴らして加算合成した音を、1つの名前付き音色として作成・試聴・保存できるエディタを制作する。

1回のノートオンで、複数の音源レイヤーを同期して発音できることを基本とする。各レイヤーは、独立した音源、基本音色、音程、開始時間、ソフトウェアエンベロープを持つ。

作成した音色はMGSC 1.11で直接コンパイル可能なMMLソースとして出力できることを必須とする。

### 2.1 製品名

- 正式製品名は「MGS Tone Craft」とする。
- 略称および内部識別子は`MGSTC`／`mgstc`とする。
- GitHubリポジトリ名は`mgs-tone-craft`、Windows実行ファイル名は`mgstc.exe`とする。
- アプリケーション表示名、ウィンドウタイトル、ビルドターゲット、文書表題および配布物名を正式名称と略称に統一する。
- 製品名中の`MGS`は、本製品がPSG／SCC／OPLLの複合音色制作とMGSDRV連携を主目的とすることを表す。
- 保存データ形式と設定パスには、略称に基づく固定内部識別子と形式バージョンを使用する。

## 3. 対象音源

### 3.1 SSG / PSG

- 対象チップはYAMAHA YM2149とする。
- AY-3-8910ではなく、MSXで多く利用されるYAMAHA側のSSG特性を標準とする。
- 矩形波3チャンネルを扱う。
- ノイズ生成器とハードウェアエンベロープはチップ内で共有されるものとして扱う。
- Tone / Noiseの有効・無効、固定音量、ノイズ周波数、ハードウェアエンベロープ周期・形状を編集可能にする。
- AY-3-8910互換モードは将来拡張候補とし、初期実装の標準にはしない。

### 3.2 SCC

- MGSDRVが扱う標準SCCを初期実装の対象とする。
- 5チャンネルを扱う。
- 1波形は符号付き8bit、32サンプルとする。
- 標準SCCではチャンネル4・5が波形RAMを共有する制約を再現し、競合時に警告する。
- SCC+は将来拡張候補とし、初期実装の必須要件には含めない。

### 3.3 OPLL

- 対象チップはYAMAHA YM2413とする。
- 9音メロディーモード、および6音メロディー＋リズムモードを考慮する。
- ROM音色15種と、オリジナル音色1種を扱う。
- オリジナル音色のMOD / CARパラメーターを編集できるようにする。
- オリジナル音色レジスタ0～7はOPLL全体で共有される資源として扱う。
- 複数チャンネルがオリジナル音色を使う場合、レジスタ変更が全該当チャンネルへ影響することを再現し、競合時に警告する。

## 4. 採用するエミュレータ

音源エミュレーションにはDigital Sound Antiquesの以下のCライブラリを採用する。

| 音源 | ライブラリ | ライセンス |
|---|---|---|
| YM2149 | `digital-sound-antiques/emu2149` | MIT |
| SCC | `digital-sound-antiques/emu2212` | MIT |
| YM2413 | `digital-sound-antiques/emu2413` | MIT |

MAMEの音源エミュレータは製品には使用しない。

導入時には各ライブラリの使用コミットを固定し、ライセンス表示を同梱する。

### 4.1 YM2149の基本設定

YM2149は次の構成を基準とする。

```c
PSG *psg = PSG_new(3579545, sample_rate);
PSG_setClockDivider(psg, 1);
PSG_setVolumeMode(psg, 1);
PSG_reset(psg);
```

### 4.2 YM2413の基本設定

YM2413は次の構成を基準とする。

```c
OPLL *opll = OPLL_new(3579545, sample_rate);
OPLL_reset(opll);
OPLL_setChipType(opll, 0);
OPLL_resetPatch(opll, OPLL_2413_TONE);
```

### 4.3 共通制御方針

- GUI操作、ソフトウェアエンベロープ、MGSDRV互換プレビューは、共通の時刻付きレジスタイベント列へ変換する。
- OPLLのレジスタイベントは`OPLL_writeReg()`へ渡す。
- SSGのレジスタイベントはemu2149のレジスタ書き込みAPIへ渡す。
- SCCの波形・周波数・音量・キー状態はemu2212へ渡す。
- エディタ独自の簡易音源を併用せず、保存データ、MGSC出力、試聴で同じ値を使用する。
- MGSDRVのカウント境界でイベントを適用する。

### 4.4 音源エンジンと60Hz実行器

詳細設計は`ENGINE_DESIGN.md`を基準とする。

- エンジンコアはUIおよびWindows音声APIから独立したC++20ライブラリとする。
- エンジン内部サンプルレートは48,000Hz、1論理カウントは正確に800サンプルとする。
- MGSDRV意味実行層が`@e`・`@r`・キー・音量・共有状態を処理し、時刻順の物理レジスタ書き込み列を生成する。
- PSG、SCC、OPLLは各1チップのインスタンスを持ち、チャンネルごとに独立したチップを生成しない。
- 同一カウント内の途中書き込みと順序を保持するが、書き込み間に実機Z80相当のサブフレーム時間は挿入しない。
- Windows音声コールバックの長さにかかわらず、800サンプルごとの境界で次カウントを適用する。
- UIから音源APIを直接操作せず、不変スナップショットとリアルタイム安全なコマンドキューを介する。
- GUI試聴、レジスタ回帰テストおよびMGSC出力照合は同じ意味実行器を使用する。
- 内部の音高検索キーにはMIDIノート番号と同じ整数配置を利用できるが、これは
  MIDI音源、MIDI出力またはMIDI準拠UIを意味しない。画面では`C4`、`C#4`等の
  音名を表示し、MGSDRV音高テーブルから各音源の周期／F-Numberへ変換する。
- UI→音声と音声→UIには、それぞれ固定容量の単一生産者・単一消費者キューを使用する。
- 音声コールバック先頭で待機中コマンドを適用し、キュー操作中にロックまたは動的メモリ確保を行わない。
- キュー満杯は呼び出し側へ即時通知し、不正コマンド、レンダー失敗およびクリップ状態変化は音声→UIキューへ返す。
- 不変プログラムは3スロットの事前確保プールで管理し、UIが編集できるのは`Free`スロットだけとする。
- UIは構築後のスロットを`Pending`として送信し、それ以降は音声側が`Active`から`Free`へ返すまで内容へアクセスしない。
- スロットには世代番号を付け、古い世代の切替コマンドを拒否する。
- プログラム切替時に音声スレッド上で音源インスタンスの生成・破棄、ヒープ確保または参照カウント解放を行わない。
- 即時発声では、プログラム切替コマンドへ最後に手動発声した音階を含め、切替と同じ音声コールバック内でTick 0から再トリガーする。
- 切替完了は世代番号付き通知としてUIへ返し、自動発声自体では記憶音階を更新しない。
- Windows音声出力は共有モードWASAPIを使用し、48,000Hz・32bit float・ステレオで`RealtimeEngineHost`へ接続する。
- WASAPIはイベント駆動でバッファを補充し、各イベントで現在のパディングから書き込み可能フレーム数を求める。
- 既定デバイスのミックス形式が異なる場合はWindows Audio Engineの共有モードPCM変換を使用し、エンジン内部の48,000Hz時間軸は変更しない。
- 音声開始・停止、アンダーランおよびデバイスエラーを固定容量の状態キューでUIへ通知する。
- 自動テストでは利用者の音声デバイスを無断で開かず、実デバイス試験は明示的な再生操作を伴うスモーク試験として行う。

### 4.5 既定ミキサーバランス

- 既定バランスは`msxplay-js` 1.9.1（commit `6e163ef2a5bc167ede9b5e32aa021588e9415abf`）と、その依存先`libkss-js` 2.2.1を固定基準とする。
- PSG・SCC・OPLLの既定チップゲインは同一とし、比率を`1.0 : 1.0 : 1.0`とする。
- 既定マスターゲインは`3.0`とする。
- エミュレータごとの素の出力RMS差を打ち消す自動音量均一化は行わない。
- 音源別ゲインは利用者が調整できるアプリケーション設定とし、MGSC音色データへは出力しない。
- 最終出力範囲を超過した場合だけ安全クランプし、クリップ状態を表示する。
- 上流の将来版で値が変わっても暗黙に追従せず、基準更新は仕様変更として行う。

## 5. 複合音色

### 5.1 定義

複合音色は、1つ以上のレイヤーを束ねたエディタ独自の音色単位とする。

各レイヤーは最低限、次の情報を持つ。

- レイヤー名
- 音源種別
- 使用チャンネルまたは割り当て条件
- 基本音色
- 相対音程
- デチューン
- 発音開始ディレイ
- 音量エンベロープ
- 音程エンベロープ
- 音色・波形変化
- ループ範囲
- キーオフ後の動作
- 有効 / ミュート / ソロ状態

### 5.2 発音

- 1回の試聴操作で、複合音色に含まれる全レイヤーを同期発音する。
- レイヤーごとの開始ディレイと相対音程を反映する。
- 使用チャンネル数と残りチャンネル数を表示する。
- MGSDRVのトラック割り当てと共有資源の競合を常時検証する。

## 6. ソフトウェアエンベロープ

### 6.1 共通エンベロープ

ソフトウェアエンベロープはSSG専用ではなく、SSG・SCC・OPLLで共通利用できる独立した編集対象とする。

固定長の単純なステップ列ではなく、MGSCの`@e`命令列を保持できるイベントシーケンスとして内部管理する。

MGSC 1.11 / MGSDRV 3.20で利用可能なソフトウェアエンベロープ機能は、音源ごとに一部を選抜せず、`@e`と`@r`を含めてすべて実装する。音源に対して無効な命令もデータから削除せず、「MGSDRVでは無視される命令」として保持・表示・検証する。

1カウントの長さは約1/60秒で固定する。画面表示、試聴、内部イベント列、MGSC出力はすべて同じカウント時間軸を使用する。

実機MGSDRVでは、同時再生チャンネル数や同一割り込み内のレジスタ書き込み量が多い場合、Z80の処理が1/60秒の割り込み枠内に収まらず、処理順が後ろのチャンネルほど実書き込み時刻が後段へずれ込む場合がある。本エディタはこのCPU負荷由来の遅延を互換再現の対象とせず、すべての音源レーンを理想化した固定約1/60秒の論理カウント軸で処理する。

同一カウント内のイベント順序はMGSDRV互換で維持するが、Z80命令サイクル相当のサブフレーム時間差や、過負荷時の次フレームへの処理持ち越しは音声プレビューへ加えない。

OPLL、SCC、PSGの各ソフトウェアエンベロープを同一時間軸上の独立レーンとして縦に並べて表示する。複合音色内の発音開始位置、音量変化、音色切り替え、レジスタ書き込みの時間関係を三音源間で比較できるようにする。

### 6.2 `@e`で扱う命令

以下を編集・保存・再読み込み・出力できること。

- `0`～`f`: 音量を16進数で設定し、1カウント待つ。`0`が音量0、`f`が音量15。
- `<音量>:<カウント>`: 音量を設定して指定カウント待つ
- `<音量>=<カウント>`: 現在音量から指定音量まで自動変化
- `n<ノイズ周波数>`: PSGノイズ周波数変更
- `/<モード>`または`*<モード>`: PSG Tone / Noiseモード変更
- `[`と`]`: エンベロープ内ループ
- `@<音色番号>`: SCCまたはOPLLの音色変更
- `y<レジスタ>,<データ>`: OPLLまたはPSGレジスタ書き込み
- `\<n>`: 現在周波数を累積的に変更する。正数で音程を上げ、負数で音程を下げる
- `.`と`,`: 数値間の区切り

MGSC 1.11の制約として、以下を検証する。

- カウント範囲は2～239。ただし単独の16進音量指定は1カウント待ち。
- `y`のデータ範囲は0～255。
- OPLL / FMの`y`レジスタ範囲は0～56。
- PSGの`y`レジスタ範囲は0～15。
- SCCでは`y`は無効。
- `@e`データは約255文字まで。内容によって実際の格納限界が異なるため、出力時に検証する。

#### 6.2.1 MGSDRV 3.20互換スケジューラー

`@e`の試聴は、MGSDRV 3.20の`MGSDRV.COM`にあるシーケンスエンベロープ実行器の観測結果に従う。

- キーオン処理で、命令位置を先頭、現在音量を0、待ちカウンターを1へ初期化する。
- キーオン初期化後、同じ約1/60秒割り込み内のエンベロープ処理で待ちカウンターを0にし、先頭命令を実行する。PSGではキーオン用周波数設定と先頭音量が同じ割り込み内に出力される。
- `0`～`f`は音量を即時適用し、待ちカウンターを1にしてその割り込みの処理を終了する。
- `<音量>:<カウント>`は音量を即時適用する。次の命令は、指定カウント後の割り込み内で実行する。
- `<音量>=<カウント>`は開始時に現在音量を一度適用し、次の割り込みから指定回数だけ音量を更新する。
- 待ちカウンターが0になった割り込みでは、同じ割り込み内で後続命令の解釈を続行する。
- `@`、`y`、`\`、`n`、`/`、`*`、`[`、`]`は時間を進めない。同一カウント内では、直列化された順番どおりに処理する。
- PSGキーオン時は、音色定義に付属する初期Tone / NoiseモードとNoise周波数、ノートの基本周期、`@e`先頭から連続するゼロ時間命令、先頭音量の順で出力する。
- ランプ終了カウントで後続の音量命令まで到達した場合、同じ音量レジスタへのランプ終端値と後続値の2回の書き込みを省略しない。実測例`f:2.0=4.8:2`では、ランプ開始値`f`に続いて`c,8,4,0`と進み、最後の`0`と次の`8`は同じ割り込み内に出力される。
- 命令列の末尾へ達した後は、MGSDRVと同様に最終エンベロープ音量の適用処理を割り込みごとに継続する。
- 1カウント内で同じレジスタへ複数回書き込まれる場合も、互換プレビューでは途中の書き込みを省略せず順序を維持する。

音量ランプは、単純な浮動小数点補間ではなく、MGSDRVの整数剰余分配を再現する。現在音量と目標音量の差の絶対値を`D`、指定カウントを`C`、剰余を`R`とした場合、各更新で`q, R = divmod(R + D, C)`を計算し、符号付きの`q`を現在音量へ加える。これにより、割り切れない変化量もMGSDRVと同じカウントへ分配する。

#### 6.2.2 周波数変更

`\`命令は三音源で利用できる、時間を進めない周波数変更イベントとして編集・保存・試聴・出力する。

- MGSC上の範囲は`-127`～`127`。
- 複数の`\`命令は同じカウントに配置でき、記述順に累積する。
- OPLLでは内部F-Number側へ符号付き値を加算する。
- PSGとSCCでは周期値が音程と逆方向に変化するため、MGSDRVと同様に符号を反転して内部周期値へ加算する。
- 変更後の内部周波数値をトラック状態へ保存し、後続の`\`命令は変更後の値を基準にする。
- UIでは「生の周期増減」ではなく、MGSCと同じく正数で音程上昇、負数で音程下降として表示する。

### 6.3 `@r`

`@r<number> = { Mode,Noise,AL,AR,DR,SL,SR,RR }`形式を編集・出力できるようにする。

- 各値の範囲は0～255。
- `@e`と`@r`は同じ番号領域を使用する。
- 同一番号で両方を定義しない。
- UI上ではシーケンス型`@e`とレート型`@r`を排他的に切り替える。
- 自由編集の既定モードは`@e`とし、共通エンベロープ全体をADSR型へ制限しない。
- `@r`は一般的な4パラメーターADSRと表記せず、MGSDRV固有の`AL / AR / DR / SL / SR / RR`をそのまま表示する。
- `@r`専用画面では6個の数値編集、縦バーおよび実行結果の曲線プレビューを同期させる。
- 共通時間軸には、`@r`を約1/60秒単位で実行した結果を描画する。ただし保存データは6個の元パラメーターを維持する。
- `@r`から`@e`への変換は、ユーザーが明示的に選択した場合のみ「ステップ列へ展開」として提供する。
- 任意の`@e`から`@r`への自動変換は、音色変更、周波数変更、`y`およびループを失うため提供しない。

MGSDRV 3.20互換プレビューでは、ALをキーオン時の8bitレベルとして開始し、ARで255まで加算、DRでSLまで減算、SRでサステイン中に0まで減算する。PSG/SCCのキーオフがSustain中なら次の処理からRRで0まで減算する。AttackまたはDecay中のキーオフはRelease予約として保持し、SLへ到達してSustainへ移った次の処理からRRを適用する。OPLLのキーオフはYM2413のハードウェアEGへ出力し、同じキーオフでは`@r`のソフトウェアReleaseへ移行しない。8bit内部レベル`L`とトラック音量`V`から音源へ渡す前段の0～15音量を`floor(L × (V + 1) / 256)`で生成する。

実MGSC生成の`TEST_RATE.MGS/VGM`でも、`AL=0, AR=64, DR=32, SL=128, SR=8, RR=16, v15`に対し、PSG・SCC・OPLLはキーオンから`4, 8, 12, 15, 13, 11, 9, 8`の同一論理音量を出力した。8カウント目のゲート終了後、PSG/SCCは次のカウントからRRにより`7, 6, 5, 4, 3, 2, 1, 0`、OPLLは同じ8カウント目にキービットを解除し、RRへ移らずSRにより`7, 7, 6, 6, 5, 5, 4, 4, 3, 3, 2, 2, 1, 1, 0`を出力した。この量子化結果まで互換プレビューの受け入れ基準とする。

`@e`の0～15現在音量`E`は、トラック音量`V`と`max(0, E + V - 15)`で合成する。`@e`に`@r`の乗算式を流用しない。`@e` / `@r`とも、その前段音量からマスター減衰値とトラック追加減衰値を順に減算し、0を下限とした値を最終論理音量とする。PSG/SCCはこの値を音量として出力し、OPLLは下位4bitを`XOR 15`して減衰ニブルへ変換する。

### 6.4 PSGハードウェアエンベロープとの切り替え

MGSCの`s`と`m`は`@e`定義内の命令ではなく、演奏トラック側のコマンドとして保持・出力する。エディタではソフトウェアエンベロープと同じ共通時間軸上へ配置できる制御イベントとして扱い、MGSDRV 3.20と同じ共有状態・実行順でプレビューする。

- `m1`～`m65535`はYM2149で共有されるEG周期を変更し、レジスタ11・12へ即時出力する。
- `s0`～`s15`は共有形状を保存し、そのPSGトラックのハードウェアEG選択をON、トラック音量を15にする。`s`実行時点ではEGを再始動しない。
- ハードウェアEG選択中のトラックがキーオンしたとき、共有形状をレジスタ13へ書いてEGをチップ全体で再始動し、そのチャンネルの音量レジスタへ`10H`を出力する。
- マスター減衰とトラック追加減衰の合計が8以上の場合、MGSDRV互換プレビューではキーオン時のレジスタ13再書き込みを抑止する。音量レジスタの`10H`選択は維持する。
- 全体出力無効時は、形状再始動と音量書き込みを行わない。
- PSGの`v`実行で、そのトラックのハードウェアEG選択をOFFにする。
- `@e`または`@r`の選択だけではハードウェアEG選択を解除しない。`v`で解除するまで、キーオン時はハードウェアEG経路を優先する。
- 周期・形状・EG位相はPSG 3チャンネル共有、ハードウェアEGを使うかどうかはチャンネル別とする。
- 同一カウントで複数トラックが共有値やレジスタ13を書いた場合は、MGSDRVのPSGトラック処理順（チャンネル1から3）の後勝ちとする。

再キーオン時、`@e`は待ち1、ランプなし、命令位置0、現在音量0へ戻し、`@r`は現在レベルをAL、フェーズをAttackへ戻してRelease予約を解除する。PSGハードウェアEG選択状態は保持する。

## 7. OPLLリアルタイム音色変化

### 7.1 目的

`@e`内の`y`コマンドを利用し、発音中にYM2413のオリジナル音色を時間変化させる。

初期表示するオートメーション対象は次とする。

- MOD Total Level
- Feedback

MOD Total LevelとFeedbackには、それぞれ独立したオートメーション有効・無効スイッチを設ける。無効な項目はプレビュー用レジスタイベントおよびMGSCの`y`命令へ出力しない。

詳細編集では次も対象にできる構造とする。

- MOD / CAR Multiplier
- Attack / Decay
- Sustain Level / Release
- AM
- Vibrato
- EG-TYP
- KSR
- KSL
- Waveform

### 7.2 レジスタ合成

`y`はパラメーターフィールドではなく8bitレジスタ全体を書き換える。このため、内部にYM2413レジスタ0～7のシャドー値を持つ。

MGSDRV自身の`y`実行処理はレジスタ番号とデータを音源へ直接送信し、レジスタ0～7のソフトウェアシャドーを合成更新しない。エディタのシャドー値は、複数フィールドのGUI編集を安全に8bitへ合成するためのオーサリング状態であり、MGSDRV内部状態を示すものではない。直後の`@`でオリジナル音色を選択すると8レジスタが再ロードされ、それ以前の`y`変更は上書きされる。

出力処理は次の順序とする。

1. `@v`音色からレジスタ0～7の初期値を生成する。
2. 時刻ごとのオートメーション値を計算する。
3. 同時刻に同一レジスタへ属する変更を合成する。
4. 変更対象外のビットを保持する。
5. 完成した8bit値を`y<reg>,<data>`として出力する。
6. 同一値の冗長な連続書き込みを削除する。

MOD Total Levelはレジスタ`$02`の下位6bit、Feedbackはレジスタ`$03`の下位3bitとして扱う。

MOD Total Levelは出力音量ではなく変調波の減衰量である。0～63、1段階0.75dBとして表示し、値が大きいほど変調が弱くなることをUIで明示する。

全体音量の時間変化にはMGSDRVのソフトウェア音量エンベロープを使用する。

### 7.3 Original Tone Bus

OPLLオリジナル音色のオートメーションは、個別チャンネルではなく共有の`OPLL Original Tone Bus`として管理する。

- 同じBusを利用するチャンネルは同じ音色変化を受ける。
- ROM音色を利用するチャンネルには影響しない。
- 独立した複数のオリジナル音色モーフを同時実行しようとした場合は競合として扱う。
- 別レイヤーによる`@v`音色切り替えがオートメーションを上書きする場合も競合として扱う。

### 7.4 OPLLパッチスライド

OPLLパッチスライドは、ノートのキーオン状態を維持したまま、時間軸上の指定カウントで音色番号を離散的に切り替える技法とする。音色間のパラメーター補間、複数チャンネル間のクロスフェード、`y`によるレジスタオートメーションとは区別する。

パッチスライドはOPLLレイヤーごとの独立したイベントレーンとして管理し、各イベントは最低限、次の情報だけを持つ。

- 適用カウント
- 切り替え先の音色ID
- ROM音色またはオリジナル音色の区別

必要な編集操作は次のとおり。

- 任意位置への音色イベント挿入
- 移動、複製、削除、並べ替え
- ROM音色とオリジナル音色を混在させたシーケンス作成
- イベント単位およびシーケンス全体の有効・無効切り替え

パッチ音色イベントにはキーオン指定を持たせない。ソフトウェアエンベロープ内にはキーオフ命令がなく、ノートのキーオン・キーオフは演奏トラック側が管理するためである。試聴およびMGSC出力では、音色変更時にキーオフまたは再キーオンを発生させない。ROM音色の切り替えは対象チャンネルの音色番号を変更し、周波数、位相および進行中の発音状態を保ったまま適用する。

パッチ音色イベント自体には保持カウントを持たせない。ある音色は次の音色イベントが発生するまで結果として継続するが、その期間はイベントの属性ではなく、共通時間軸上の次イベント位置から導出する。

MGSC出力では`@e`内の`@<音色番号>`として直列化する。`@`命令自体は時間を進めないため、時間の進行は同じソフトウェアエンベロープに含まれる音量命令のカウントによって表現する。パッチイベントのためだけの保持カウント命令を生成しない。

### 7.5 パッチスライドとレジスタオートメーションの同時使用

1つのOPLLレイヤーで、パッチスライドと`y`レジスタオートメーションを同時に利用できることを必須とする。

- パッチスライドはチャンネル固有の音色選択イベントとして扱う。
- オリジナル音色レジスタ0～7の`y`オートメーションは、共有の`OPLL Original Tone Bus`イベントとして扱う。
- 両レーンは同一のMGSDRVカウント時間軸を使用する。
- MGSDRVは同一カウント内の`@`と`y`をエンベロープへ直列化された順番どおりに実行する。
- UI上で同一カウントにパッチイベントとオートメーションイベントを新規作成した場合の既定直列化順は、音色選択を先、`y`レジスタ書き込みを後とする。
- インポート済みまたは詳細編集で順番が明示されている場合は、その順番を保存してプレビューする。
- 既定の直列化順により、オリジナル音色へ切り替えた時点では、切り替え先`@v`の初期値を基準に`y`オートメーション値を上書きする。逆順に直列化した場合は、後続の`@`による8レジスタ再ロードが先行する`y`を上書きする。
- そのカウントに必要な全イベントを適用した後で、音量・カウント命令により時間を進める。
- パッチスライド中も`y`オートメーションの再生位置を停止またはリセットしない。

実MGSC/MGSDRVの`TEST_AUTOMATION.MGS/VGM`では、発音中の`@17 y2,20 y3,02 c:2`が、YM2413レジスタ0～7の昇順ロード、対象チャンネル音量レジスタへの減衰`F`、レジスタ2への`14H`、レジスタ3への`02H`、現在論理音量`c`に対応する減衰`3`の順で出力された。キーオンレジスタへの書き込みは発生しなかった。互換プレビューでは、オリジナル音色切替時のこの一時消音と、同一カウント末尾の音量再出力も再現する。

同じテストのSCC `@1 c:2`では、対象波形RAMへ32バイトをアドレス0～31の順で転送した後、対象チャンネル音量`c`を出力した。SCC音色切替でもキーオン／キーオフは行わない。

ROM音色を選択しているチャンネルでは、オリジナル音色レジスタ0～7への`y`書き込みはそのチャンネルの音に直接作用しない。ただし、同時にオリジナル音色を使用している別チャンネルには作用するため、イベントを削除せず共有Busの状態として継続する。

パッチスライドにオリジナル音色が複数含まれる場合、各`@v`切り替えによるレジスタ0～7の初期値ロードと、同時刻以降の`y`オートメーションを順序どおり再現する。別レイヤーもオリジナル音色を使用中であれば共有レジスタ変更として警告する。

プレビュー用イベント列とMGSC出力イベント列は、同じマージ処理から生成する。テストでは最低限、次を確認する。

- スライド中の音色変更でキーオンビットを解除・再設定しない。
- 同一カウントでは`@`相当の音色選択後に`y`相当のレジスタ書き込みが行われる。
- ROM音色区間でもOriginal Tone Busの時間が進行する。
- オリジナル音色へ戻った時点で、その時刻のオートメーション状態が反映される。
- GUI試聴とMGSC出力でイベント順序と待ちカウントが一致する。

### 7.6 9音リズムモード

MGSDRVを9音メロディーモードで動作させたまま、YM2413レジスタ`0EH`のリズムモードbitを`y14,32`で設定し、音源側だけをリズムモードとして扱う高度機能を「9音リズムモード」と呼ぶ。MGSDRV標準の6音＋専用リズムトラックとは区別し、ソフトウェアエンベロープを利用する複合音色ではこちらを使用する。

- レジスタ`0EH`の下位5bitを`y`で操作してBass Drum、Snare Drum、Tom、Cymbal、Hi-Hatを発音する。
- 同じ打楽器の再トリガーでは対象bitを一度OFFにしてからONにする`y`列を生成する。
- 物理OPLLチャンネル7～9はリズム用レジスタへ転用されるため、通常メロディーとの同時割り当てを競合として警告する。
- レジスタ`36H`の下位ニブルはBass Drum音量、`37H`の上位／下位ニブルはHi-Hat／Snare Drum音量、`38H`の上位／下位ニブルはTom／Cymbal音量として扱う。
- MGSDRVが通常の音色番号として書く上位ニブルは、音源側ではOPLL Ch.8の`@`がHi-Hat音量、Ch.9の`@`がTom音量として解釈される。
- OPLL Ch.7のソフトウェア音量はBass Drum、Ch.8はSnare Drum、Ch.9はCymbalの下位ニブルを変化させる。
- Hi-Hat／Tomの上位ニブルは減衰値であり、値が小さいほど大音量になる。ただし通常のMML音色番号`@0`～`@14`は、MGSC/MGSDRVによりYM2413の音色ニブル`1`～`15`へ1ずらして変換される。9音リズムモードでは、これが物理減衰`1`～`15`、聴感上の論理音量`14`～`0`に対応する。
- したがって安全な`@0`～`@14`だけではHi-Hat／Tomの論理最大音量`15`（物理減衰`0`）を表現できない。UIでは論理音量、MMLの`@`値および実レジスタニブルを併記する。

`y14,32`によるリズムモード切替は、Fチャンネル（OPLL Ch.7）のトラック先頭に置き、原則1回だけ行う。個々の音色やノートのソフトウェアエンベロープ内には置かない。切替時にはYM2413内部の状態により意図しない打楽器発音が生じるため、直前と直後へそれぞれ`v0r2`の無音ガード区間を必ず置く。

- Fチャンネル先頭の生成MML基本形は`F v0r2 y14,32 v0r2 ...`とする。
- 他のOPLL Ch.7～9レイヤーも同じ長さだけ待機させ、切替ガード終了後に同期して音色処理を開始する。
- `y14,32`を各音色の`@e`へ埋め込んだり、試聴ノートごとに再実行したりしない。
- プレビューエンジンではリズムモード状態をセッション単位で保持し、既に切替済みなら再度切り替えない。
- 切替後のレジスタ`0EH`書き込みは、リズムモードbitを保持したまま打楽器キーbitだけを操作する。
- MGSDRVは9音メロディーモードのままなので、F/G/Hのノート処理では通常どおりYM2413レジスタ`26H`～`28H`へ周波数・キービットを書く。これらをエディタ側で削除せず再現する。ただしYM2413側ではリズムモード中の打楽器キー状態をレジスタ`0EH`で制御する。

打楽器音量指定としての`@15`は使用禁止とする。MGSDRVでは`@15`がオリジナル音色ロード経路へ入り、共有レジスタ`0～7`を@15の音色データで書き直す。その結果、同じオリジナル音色番号0を利用している他チャンネルの`@16`以降も@15の音色へ変化するためである。

- エディタは9音リズム用の音量イベントとして`@15`を自動生成しない。
- `@#`の自動再割り当てだけでこの副作用を回避できるとは仮定しない。
- Hi-Hat／Tomの消音には安全な`@14`（物理減衰`F`）、またはペアとなる下位ニブルを保持したレジスタ`37H`／`38H`への`y`書き込みを使用する。
- `@15`が本来生成する物理減衰`0`＝論理最大音量`15`が必要な場合は、ペアとなる下位ニブルを保持した`y`レジスタ書き込みで上位ニブルを`0`にする。`@15`は使用しない。
- インポートしたデータに9音リズム音量用途の`@15`が存在した場合は削除・置換せず保持するが、オリジナル音色共有破壊のエラーを表示し、明示的に修正されるまで安全な出力とは扱わない。
- 通常のOPLLパッチスライドで用いる`@15`まで一律禁止せず、9音リズムのHi-Hat／Tom音量レーンに限定して禁止する。

## 8. SCC波形編集

### 8.1 基本編集

- 32サンプルをグラフ上で直接描画できる。
- 各値を符号付き10進数および8bit 16進数で確認・編集できる。
- 値域は-128～127とする。
- 正規化、平滑化、反転、基本波形生成などの補助操作を提供する。
- MGSC出力では各値を`00`～`ff`の8bit 16進数へ変換する。

波形グラフの直下に、32個のサンプル数値入力欄を横一列で配置する。各数値入力欄の中心と、対応する波形グラフのサンプル位置の縦軸を一致させる。

- グラフのサンプル0～31と数値入力欄0～31を常に同じ横位置へ揃える。
- 横幅が不足する場合は、グラフと数値入力欄を一体として同じ倍率で拡大・縮小または横スクロールする。
- 数値はキーボードから直接入力できる。
- 符号付き10進数と8bit 16進数のどちらから変更しても、同じサンプル値として双方向同期する。
- 入力途中の不正値は確定せず、有効値だけを波形、試聴音および`@s`プレビューへ反映する。

波形グラフはマウスでなぞって描画できる。ドラッグ中にポインターが複数サンプル位置を飛び越えた場合は、直前位置と現在位置の間を補間して埋め、描画速度による欠落を防ぐ。ペン固有のWindows Pointer入力対応は要件としない。

### 8.2 プリセット波形と加工

SCC単音色エディタに、最低限、次の基本波形プリセットを用意する。

- Sine
- Square
- Triangle
- Saw Up
- Saw Down
- Pulse

プリセット適用時に、生成する波形の倍音倍率を変更するバーを表示する。最低限、`1.0x`、`1.5x`、`2.0x`を選択でき、変更中は適用結果をプレビューする。倍率は32サンプル内へ描画するプリセット波形の周期数へ反映する。

初期ネイティブ実装では倍音倍率を3位置の横バーとして表示し、右側へ現在の`1.0x`、`1.5x`または`2.0x`を表示する。プリセット種類またはバー位置が変わった時点で、現在の32サンプルを変更せず候補波形を再生成する。

32サンプルの循環波形では非整数周期が単一の倍音にならないため、`1.5x`を32サンプル内の1.5周期として直接生成しない。`1.5x`は、位相を揃えた`1.0x`波形と`2.0x`波形の50%倍音ブレンドとして生成する。画面には、この意味をツールチップまたは補足表示する。

プリセット領域に`現在の波形とマージ`チェックボックスを表示する。

- OFF: 生成したプリセットで現在の32サンプルを置き換える。
- ON: 現在編集中の32サンプルを保持し、生成したプリセットとマージする。
- マージ結果は-128～127へ収める。
- 適用前の波形へ戻せるよう、1回のUndo単位として扱う。

標準マージ方式は、`位相合わせ付き・音量正規化リニアマージ`とする。編集波形を`A[32]`、プリセット波形を`B[32]`、マージ量を`m`（0.0～1.0）として、次の順序で処理する。

1. 符号付き8bit値を浮動小数点へ変換する。
2. `A`と`B`からそれぞれ平均値を引き、DC成分を除去する。
3. `B`を0～31サンプル循環シフトし、`A`との正規化相互相関が最大になる位置を選ぶ。
4. 極性反転を許可している場合は、`B`と`-B`の両方を比較し、相関が高い方を採用する。
5. 音量維持が有効な場合は、`B`のRMSを`A`のRMSへ合わせる。
6. `M[i] = A[i] * (1 - m) + B[i] * m`でリニアマージする。
7. `M`から平均値を引き、再度DC成分を除去する。
8. 目標RMSを維持しつつ、ピークが範囲を超える場合は32サンプル全体を同じ倍率で縮小する。
9. 四捨五入し、-128～127へ量子化する。

個別サンプルのハードクリップは波形形状を変えるため、範囲超過時は全体スケーリングを優先する。`A`または`B`が無音に近くRMS計算が成立しない場合は、存在する側のRMSを使用する。

プリセットのマージ領域には最低限、次を表示する。

- マージ量: 0～100%
- 位相を自動調整: 初期ON
- 極性反転を許可: 初期ON
- 音量を維持: 初期ON
- 適用前波形と適用後波形の重ね表示
- 適用前と適用後の交互試聴
- 適用、キャンセル、Undo

マージ量および各オプションの変更中は、確定前の浮動小数点結果を8bitへ量子化してプレビューし、`即時発声`がONなら変更のたびに同じ`Last Audition Note`で発声する。

プリセット／マージプレビューは次の状態を分離して扱う。

- 編集中の確定波形は従来の緑色実線、量子化済み候補波形は橙色破線として同じグラフへ重ねて表示する。
- プリセット種類、倍音倍率バー、置換／マージ、マージ量、位相自動調整、極性反転許可または音量維持を変更するたびに候補だけを再計算する。
- 候補の再計算では32個のDEC／HEX欄、ライブラリ音色、MGSC出力およびUndo履歴を変更しない。
- `即時発声`がONの場合は候補波形を一時的な試聴プログラムとして同じ`Last Audition Note`で発声するが、エディタの確定32バイトへ書き戻さない。
- `適用`は現在表示中の候補を確定波形へ一度だけコピーし、適用前波形を1回のUndo対象にする。
- `プレビュー取消`は候補表示を破棄し、確定波形を変更しない。即時発声がONの場合は確定波形を再発声して聴感も元へ戻す。
- プレビュー中に波形の直接描画、DEC／HEX編集、加工、MGSC／ライブラリ読込または編集取消で確定波形が変化した場合は、古い候補表示を破棄する。
- 候補が存在する間は`A/B試聴`を有効にし、A＝現在の確定波形、B＝候補波形として、ボタンを押すたびに同じ`Last Audition Note`で交互に再発声する。
- A/B試聴はユーザーが明示的に押す手動試聴であるため、`即時発声`チェックがOFFでも利用できる。チェック状態そのものは変更せず、各A/B発声は通常の確認再生と同様に1秒後にキーオフする。
- ボタン表示は次に試聴できる側を`A 原音を試聴`または`B 候補を試聴`として示し、状態欄には現在鳴らした側を表示する。
- 適用、プレビュー取消または確定波形の別操作によって候補がなくなった場合はA/B試聴を無効化し、次の候補生成時はB候補側から開始する。

`平均化`ボタンを設ける。初期方式は、波形が循環することを考慮した隣接3サンプルの移動平均とする。

```text
out[i] = round((in[(i + 31) % 32] + in[i] + in[(i + 1) % 32]) / 3)
out[i] = clamp(out[i], -128, 127)
```

- 1回の押下で32サンプル全体へ1回適用する。
- 複数回押すことで段階的に平均化できる。
- 1回の押下を1回のUndo単位として扱う。

### 8.3 SCCモーフィングの定義

SCCモーフィングは、発音中に複数のSCC波形を順番に切り替え、倍音構成を時間変化させる技法として扱う。

ホスト側で連続的に補間された音を直接出力するのではなく、確定した中間波形を`@s`として定義し、MGSDRVの`@e`内にある`@<音色番号>`で切り替える。

### 8.4 モーフシーケンス

モーフシーケンスは任意数のキーフレームを持つ。

キーフレームには次の2種類がある。

- 手動キーフレーム: ユーザーが直接作成・編集した波形
- 自動生成フレーム: 前後の手動キーフレームから補間生成した波形

必要な操作は次のとおり。

- 開始波形と終了波形の指定
- 中間波形数の指定
- 任意位置への手動キーフレーム挿入
- 波形の直接編集
- フレームの並べ替え
- フレームの複製・削除
- 自動生成フレームの固定
- 手動フレームを自動生成へ戻す
- 区間単位の再生成
- フレームごとの保持カウント指定
- A→B、B→A、往復、ループ指定

自動生成フレームを直接編集した場合、そのフレームを手動キーフレームへ変換し、以後の自動再生成で上書きしない。

### 8.5 補間

自動生成するSCCモーフ中間波形にも、プリセットマージと同じ位相整合、DC除去、RMS管理、リニア補間、全体スケーリングおよび8bit量子化処理を使用する。ただし、モーフでは開始音色と終了音色の本来の音量差を残すため、両端を同じRMSへ固定せず、区間内で目標RMSを補間する。

複数の手動キーフレームがある場合、先頭フレームを基準とし、後続の手動キーフレームを直前の位相整合済みフレームへ順番に合わせる。循環シフトと必要に応じた極性反転は、モーフシーケンス内だけで使う派生コピーへ適用し、ライブラリに保存された元の単音色データを変更しない。

各手動キーフレーム間の区間について、DC除去後の開始波形を`A`、位相整合済み終了波形を`B`とし、それぞれのRMSを`rmsA`、`rmsB`とする。補間カーブ適用後の位置を`u`（0.0～1.0）として、次の処理を行う。

```text
An[i] = A[i] / rmsA
Bn[i] = B[i] / rmsB

shape[i] = An[i] * (1 - u) + Bn[i] * u
targetRms = rmsA * (1 - u) + rmsB * u
M[i] = shape[i] * targetRms / rms(shape)

M[i] = removeDC(M[i])
M = scaleWholeWaveIfPeakExceedsRange(M)
value[i] = round(clamp(M[i], -128, 127))
```

無音に近いキーフレームではゼロ除算を避け、存在する側の正規化波形から無音側へ振幅を補間する。形状補間の途中でRMSが極端に小さくなる場合も、直前の有効な位相整合結果を使用し、不定な増幅を行わない。

最低限、以下の補間カーブを提供する。

- Linear
- Ease In
- Ease Out
- Ease In / Out
- Step

モーフ上の位置と再生時の保持時間は別に管理する。

位相自動調整は自動生成フレームで初期ONとし、区間ごとにOFFへ切り替えられるようにする。自動結果が意図した音色変化と異なる場合は、ユーザーが循環シフト位置と極性を手動指定できる。

手動キーフレームを追加・移動・削除した場合は、その地点以降の位相整合と自動生成フレームを再計算する。手動キーフレーム自体は変更せず、モーフ用の派生フレームだけを更新する。

往復モーフでは、生成済みのフレーム列を逆順に再利用し、帰路で別の位相整合を行わない。ループモーフでは末尾から先頭への接続も評価対象に含め、全区間の相関が最も高くなる循環シフトと極性の組み合わせを選ぶ。ループ接続部に大きな差が残る場合は警告する。

プレビュー、MGSC出力用`@s`生成、ライブラリへのモーフ条件保存は同じ補間処理を使用する。

### 8.6 MGSC出力

手動・自動を問わず、確定した各フレームを`@s`として出力する。

```text
@s00 = { ...32 bytes... }
@s01 = { ...32 bytes... }
@s02 = { ...32 bytes... }

@e00 = {
  0,0,
  @0 f
  @1 f
  @2 f:4
}
```

- 同一波形が複数回現れる場合、同じ`@s`番号を再利用できる。
- SCC音色番号0～31の使用数を常時表示する。
- 32音色を超える場合はエラーとする。
- 標準SCCのチャンネル4・5共有により、片方のモーフが他方へ影響する場合は警告する。

## 9. 音色変化方式の区別

複合音色で扱う音色変化は、次の方式を別機能として管理する。

- SCCモーフィング: 確定した中間`@s`波形を発音中に順次切り替える。
- OPLLパッチスライド: キーオンを維持したまま、OPLL音色番号を離散的に切り替える。
- OPLLレジスタオートメーション: `y`によりオリジナル音色レジスタ0～7を時間変化させる。
- レイヤークロスフェード: 複数チャンネルの音量を相反方向に変化させる加算合成方式。

OPLLパッチスライドとOPLLレジスタオートメーションは、同一レイヤー、同一ノートおよび同一時間軸上で併用できる。どちらかを有効にしたことを理由に、もう一方を無効化しない。

全音源共通の音色遷移手段として、2レイヤー間の音量クロスフェードも提供するが、これをパッチスライドまたはレジスタオートメーションの代替として扱わない。

実機で連続値を直接設定できないパラメーターは、エディタ側で段階値へ量子化する。量子化精度を下げる場合、ユーザーへ結果と理由を提示し、無断で変更しない。

## 10. 音色ライブラリ

### 10.1 基本要件

作成した音色を名前付きで複数保存できるローカルライブラリを提供する。

ライブラリ項目は次のカテゴリに分ける。

- OPLL単音色: 1つの`@v`に相当するオリジナル音色
- SCC単音色: 1つの`@s`に相当する32サンプル波形
- 複合音色: 単音色、ソフトウェアエンベロープ、パッチスライド、モーフおよび複数レイヤーを組み合わせた音色

最低限、以下の操作を提供する。

- 新規作成
- 保存
- 別名保存
- 複製
- 名前変更
- 削除
- 読み込み
- 名前検索
- タグによる整理

### 10.2 保存情報

各ライブラリ項目には最低限、次を保存する。

- 一意な内部ID
- 音色名
- タグ
- メモ
- お気に入り状態
- 作成日時
- 更新日時
- データ形式バージョン
- ライブラリ項目のカテゴリ
- OPLL単音色の全パラメーター
- SCC単音色の32サンプル波形
- 複合音色の全レイヤー
- SCC波形とモーフ生成条件
- OPLLパラメーターとオートメーション
- SSG設定
- `@e` / `@r`エンベロープ
- 試聴用の音程・音長

MGSCの音色番号はライブラリの主キーにしない。ライブラリ内では一意な内部IDで参照し、MGSC出力時に0～31の番号へ割り当てる。

OPLL単音色とSCC単音色の名前は、ユーザーが音色番号と無関係に任意指定できる。単音色エディタではMGSC音色番号を表示・編集せず、音色の選択と参照には内部IDを使用する。

初期ネイティブ実装では、単音色ライブラリを`%LOCALAPPDATA%\MgsToneCraft\timbre-library-v1.mgstc`へ保存する。`MgsToneCraft`は永続データ用の固定内部識別子とし、形式バージョンはファイル名とファイル内容の両方で管理する。

- ファイル先頭にマジック文字列とスキーマバージョンを持たせ、未対応版を別形式として誤読しない。
- OPLL／SCCをカテゴリで分離し、64bit内部ID、名前、タグ、メモ、お気に入り、作成日時、更新日時、データ形式版および音色データを保存する。
- 名前、タグおよびメモはUTF-8を可逆な16進表現へ変換し、タブ、改行および日本語を含む場合もレコード境界を壊さない。
- OPLLは意味パラメーターと共通変換したレジスタ0～7、SCCは32バイト波形を保存する。MGSC音色番号は保存しない。
- 保存時は同じディレクトリの一時ファイルへ全内容を書き、フラッシュ完了後に既存ファイルを置換する。書き込み途中の内容で正常な既存ライブラリを上書きしない。
- OPLL／SCC単音色エディタのモデルレスなサイドパネルから、一覧、名前、タグ、メモ、お気に入り、新規、読込、保存、別名保存および削除を操作できる。別名保存を複製操作、名前を編集して保存する操作を名前変更として利用できる。
- 一覧は音源カテゴリごとに表示し、別カテゴリの音色を誤って同じエディタへ読み込まない。
- 検索欄は大文字・小文字を区別せず、名前またはタグの部分一致で現在カテゴリの一覧を絞り込む。
- ライブラリパネルから外部`.mgstc`ファイルをインポートし、一覧で選択した1音色を`.mgstc`ファイルへエクスポートできる。
- 選択エクスポートはスキーマヘッダーと選択項目1件だけを書き出し、OPLL／SCCカテゴリ、名前、タグ、メモ、お気に入りおよび音色データを保持する。
- インポートは同形式に含まれる1件または複数件のOPLL／SCC音色を受け入れる。全項目へローカルライブラリの新しい内部IDとインポート日時を割り当て、外部ファイルの内部IDをローカルIDとして再利用しない。
- 同じカテゴリに同名の音色が既にある場合、既存項目を上書きせず、インポート項目へ`名前(1)`、`名前(2)`のように未使用になるまで連番を追加する。外部ファイル内で同名項目が連続する場合も同じ規則を順番に適用する。
- 別カテゴリの同名音色は競合としない。たとえばOPLLの`Lead`とSCCの`Lead`はそれぞれ元の名前で保持できる。
- ファイル全体の構文と版を追加前に検証し、不正な場合は1件もローカルライブラリへ追加しない。

複合音色の永続化、複製専用ボタンおよび保存ファイルの将来版移行は、同じライブラリモデルを拡張して実装する。

## 11. MGSCテキスト出力

### 11.1 完全な`.MUS`出力

選択した複合音色に必要な定義と試聴トラックを含む、MGSC 1.11でそのままコンパイル可能なソースを出力する。

- `#opll_mode`を必ず最初に出力する。
- 必要に応じて`#title`を出力する。
- SCC波形を`@s<number>`として出力する。
- OPLLオリジナル音色を`@v<number>`として出力する。
- ソフトウェアエンベロープを`@e<number>`または`@r<number>`として出力する。
- 複合音色の各レイヤーを対応トラックへ割り当てる。
- 試聴用MMLを含める。
- 最後に`#end`を出力できるようにする。

標準のトラック割り当てはMGSC 1.11に従う。

#### OPLLモード0

- トラック1～3: PSG
- トラック4～8: SCC
- トラック9～H: OPLL

#### OPLLモード1

- トラック1～3: PSG
- トラック4～8: SCC
- トラック9～E: OPLL
- トラックFまたはR: リズム

### 11.2 定義部分のコピー

既存のMMLソースへ貼り付けられるよう、`@s`・`@v`・`@e`・`@r`などの定義部分だけをテキストとしてコピーできるようにする。

定義部分だけの出力は単体でコンパイル可能であることを保証しない。単体コンパイル可能性を保証するのは「完全な`.MUS`出力」とする。

### 11.3 文字コードと改行

MGSC用ファイルの標準出力は次とする。

- 文字コード: Shift-JIS互換（Windows-31J / CP932）
- 改行: CRLF
- 拡張子: `.MUS`

### 11.4 番号割り当て

- SCC音色番号は0～31。
- エンベロープ番号は0～31。
- OPLLでは標準状態で`@0`～`@14`がROM音色1～15へ対応する。
- OPLLオリジナル音色は、MGSCで有効な範囲内の任意の`@v`番号へ割り当てられる。ROM音色の既定割り当てと衝突する場合は、出力内容と影響を表示する。
- 複数のライブラリ音色を同時出力する場合、番号衝突を自動解決する。
- 自動割り当て結果を出力前に一覧表示する。
- 出力時の割り当て画面で、ユーザーが各OPLL・SCC単音色へ有効範囲内の任意の音色番号を指定できる。
- 出力番号はライブラリ音色の名前、内部IDおよび単音色エディタの状態を変更しない。
- 同じライブラリ音色を、出力ごとに異なる音色番号で読み出せる。

### 11.5 出力検証

最低限、次を検証する。

- 数値の範囲
- `@s`の32バイト数
- `@e` / `@r`の番号競合
- `@e`の文字数
- SCC音色数
- OPLLオリジナル音色の共有競合
- PSGノイズ・ハードウェアエンベロープの共有競合
- SCCチャンネル4・5の波形共有競合
- トラック数
- OPLLモードとトラック割り当て
- 未定義音色参照
- 使用されない重複定義
- MGSCで無効なコマンドと音源の組み合わせ

エディタは冗長なレジスタイベントと完全に同一な波形を削減できる。ただし、音が変わる最適化を無断で行わない。

## 12. 画面構成

初期版のメイン画面は、複合音色制作に必要な主要要素を1画面で操作できる構成とする。OPLLとSCCの単音色パラメーター編集はメイン画面へ詰め込まず、それぞれ専用の別ウィンドウで行う。

WindowsネイティブUIはWin32 API、標準コントロールおよびカスタム描画
コントロールで実装する。Qt、WinUI 3、WebView等の追加ランタイムへ依存せず、
Visual StudioのDesktop development with C++とWindows SDKだけでビルドできる
構成とする。波形、エンベロープ、鍵盤などの連続描画領域はカスタムコントロール、
ボタン、数値入力、チェックボックスなどはWindows標準コントロールを基本とする。

画面はWindowsデスクトップ用の固定基準サイズを前提とする。装飾を増やさず、パネル階層、整列、余白および少数の音源識別色によって情報を整理する。シンプルな外観を維持しながら、日常的な音色制作に必要な設定がメイン画面内へ過不足なく収まる高密度な制作ツールを目標とする。

最低限、次の領域を持つ。

- 音色ライブラリ一覧
- 複合音色名と保存操作
- レイヤー一覧
- チャンネル・共有資源使用状況
- 時間軸
- OPLL、SCC、PSGのソフトウェアエンベロープを縦に並べた共通時間軸
- 音量・音程・音色オートメーション
- OPLLパッチスライドレーンとレジスタオートメーションレーンの同時表示
- SCCモーフシーケンス編集とSCC単音色エディタを開く操作
- OPLL Original Tone BusとOPLL単音色エディタを開く操作
- SSG Tone / Noise / Hardware Envelope設定
- 共通試聴鍵盤
- MGSC出力プレビュー
- `.MUS`保存
- 定義部分のコピー
- 構文・資源競合の警告表示

画面密度を抑えるため、通常使用する項目を常時表示し、OPLLおよびSCCの全単音色パラメーターは別ウィンドウに表示する。

各音源のパラメーター編集は一部の簡易項目だけに限定しない。音源ごとに音色番号を選択し、次の全パラメーターへアクセスできる構成とする。

- OPLLメイン画面: 音色選択、音量、パッチスライド、全対応レジスタオートメーション
- OPLL単音色エディタ: 名前、MOD / CARの全音色パラメーター
- SCCメイン画面: 音色選択、モーフシーケンス、音量および周波数関連設定
- SCC単音色エディタ: 名前、32サンプル全値
- PSG: エンベロープ番号選択、Tone / Noise、音量、ノイズ周波数、ハードウェアエンベロープ周期・形状、およびMGSDRVで設定可能な全項目

通常画面では選択中音色の主要項目と専用エディタを開く入口を表示する。

### 12.1 OPLL・SCC単音色エディタの共通方針

OPLL単音色エディタとSCC単音色エディタは、メイン画面と同時に操作できるモデルレスの別ウィンドウとする。

- 単音色エディタを開いている間も、メイン画面のレイヤー、ソフトウェアエンベロープ、パッチスライド、オートメーション、試聴および保存操作を無効化しない。
- 単音色を変更しながら、メイン画面のソフトウェアエンベロープを並行して編集・試聴できる。
- 単音色エディタをモーダルダイアログとして表示せず、メインウィンドウを所有者として操作不能にする設計を採用しない。
- 1ウィンドウで1つの単音色を編集する。
- 単音色にはソフトウェアエンベロープを含めない。
- パッチスライド、`y`レジスタオートメーション、SCCモーフシーケンスも単音色には含めない。
- 単音色データに音長を保存しない。
- 試聴時は演奏ボタンまたはPCキーを押している間だけキーオンし、離したときにキーオフする。
- PSGでループするシーケンス型`@e`を試聴している場合も、キーを離したカウントで音量レジスタへ0を出力し、再キーオンまでエンベロープのループ出力を停止する。`@r`はハードウェア／ソフトウェアReleaseの既定動作を継続する。
- 固定時間の確認再生も試聴機能として提供できる。
- OPLL／SCC単音色エディタ内へ、共通`Last Audition Note`を表示した明示的な`発声`ボタンを設ける。押すと対象音源を選択し、確定中の単音色を同じ音程でキーオンして1秒後にキーオフする。この操作は手動試聴なので`即時発声`がOFFでも利用できる。
- PCキーボード、画面鍵盤またはMIDI入力で`Last Audition Note`が変化した場合は、開いている両単音色エディタの発声ボタン表記を更新する。
- 編集中の値はメイン画面からリアルタイム試聴できる。
- `適用`で元のライブラリ音色を更新する。
- `別名保存`で新しい単音色として保存する。
- `キャンセル`でウィンドウを開いた時点の値へ戻す。
- 初期ネイティブ実装では、ライブラリの`読込`、`新規`および成功した`適用`／`別名保存`を新しい編集基準点とする。`変更を取消`は、その後に変更されたOPLLレジスタ0～7またはSCC波形32バイトに加え、音色名、タグ、メモ、お気に入り状態および選択中の内部IDを基準点へまとめて戻す。
- `変更を取消`で音色データを復元した直後は、OPLL／SCCの全GUI、エンベロープ／波形グラフ、MGSCプレビューおよび試聴用音源へ復元値を同期する。SCCでは取消前の波形をUndo対象として保持する。
- MGSC定義のインポート、クリップボード貼り付け、ROMプリセット読込および手動パラメーター編集は基準点を暗黙に更新しない。内容を確定するには`適用`または`別名保存`を使用する。
- 同じライブラリ項目を複数ウィンドウで同時編集しない。既に開いている場合はそのウィンドウを前面へ出す。

OPLLおよびSCCの単音色エディタに、MGSC音色定義のインポートとエクスポートを設ける。

- OPLLは`@v<number> = { ... }`、SCCは`@s<number> = { ... }`を対象とする。
- `.MML`、`.MUS`または`.TXT`から定義部分だけでなく曲全体を読み込める。セミコロン以降の行コメントを除外し、最初に現れる対象音源の有効な定義を読み込む。
- OPLLのインポートでは`TL, FB`およびMOD／CAR各11項目を検証し、SCCでは32個の8bit 16進値を検証する。個数不足、余分な値または範囲外の値があれば現在の音色を変更せずエラーとする。
- インポート元の定義番号は読み込んだ音色の名前、内部IDまたは保存データへ取り込まない。
- エクスポート時だけ一時的な出力番号を指定する。MGSC 1.11では全音色定義番号が0～31であり、SCCは0～31、OPLLオリジナル音色として意味を持つ範囲は15～31とする。
- 出力はMGSCへ貼り付けられるASCII範囲のCP932互換テキスト、CRLF改行とし、OPLLは10進意味パラメーター、SCCは2桁8bit 16進値で出力する。
- インポート後は全GUI表示と試聴音を同期し、SCCではインポート前の波形をUndo対象にする。
- ファイル入出力と同じMGSC定義をクリップボードへコピーし、クリップボードから貼り付けてインポートできる。
- コピー時はWindowsのUnicodeテキストとANSIテキストの両形式を登録し、Unicode対応エディタと従来のCP932系エディタの双方へ貼り付けられるようにする。
- 貼り付け時はUnicodeテキストを優先し、存在しない場合はANSIテキストを使用する。ファイルインポートと同じコメント除外、定義抽出、個数・範囲検証および定義番号分離を適用する。
- クリップボードに対象テキストがない場合または不正な定義の場合は、現在の音色を変更しない。

OPLL／SCC単音色エディタの共通編集操作は次のとおりとする。

- 数値入力欄はフォーカス移動に加えてEnterキーでも値を確定し、有効値なら関連表示、音色モデル、MGSCプレビューおよび試聴へ反映する。
- 音色変更履歴はエディタごとに最大256回分を保持し、画面上の`Undo`／`Redo`と`Ctrl+Z`／`Ctrl+Y`の双方で移動できる。Undo後に新しい変更を行った場合は、その位置より後ろのRedo履歴を破棄する。
- 名前、タグ、メモ、検索、16進入力など数字以外を受け付ける入力欄へフォーカスがある間は、PCキーボード演奏割り当てを停止し、通常の文字入力および入力欄内のコピー／貼り付けを優先する。
- ボタン、チェックボックス、入力欄、コンボボックス、一覧および調整バーは`Tab`／`Shift+Tab`で順方向／逆方向に移動できる。
- 縦方向バーは値0を最下部、最大値を最上部に置く。
- 単音色エディタを表示した時点で共通画面鍵盤およびPCキーボードの試聴音源をそのエディタの音源へ切り替える。
- 入力欄以外にフォーカスがある場合、`Ctrl+C`／`Ctrl+V`は現在編集中の音色をMGSC定義としてコピー／貼り付けする。

OPLL／SCC単音色エディタに`WAV変換`ボタンを設け、WAVE音声から現在の音色を生成できるようにする。

- ファイル選択ではRIFF/WAVEのPCM 8／16／24／32bit整数、32bit IEEE floatおよび同等のWAVE_FORMAT_EXTENSIBLEを受け付け、ステレオを含む複数チャンネルはモノラルへ平均する。
- 短い単一周期WAVは収録範囲全体を1周期として扱う。約50msを超える音声は40～2000Hz相当の周期候補を自己差分で比較し、代表周期を検出して最大16周期を位相位置ごとに平均する。
- 抽出周期はDC成分を除去してピーク正規化する。解析結果として推定周波数を表示する。
- SCCでは抽出周期を循環線形補間で32サンプルへ変換し、符号付き8bitへ量子化して現在波形へ適用する。
- OPLLでは抽出周期の倍音スペクトルに加えて、循環位相および極性を合わせた時間波形形状も評価する。ROM音色、既定音色および決定的に生成した多数の広域候補から上位候補を残し、MOD／CAR MULT、MOD TL、Feedback、PMおよび両オペレーターの波形選択を複数初期点から局所探索して、知覚特徴の複合距離が最小の候補を現在のオリジナル音色へ適用する。
- 単一周期から時間エンベロープは推定できないため、WAV変換で生成するOPLL音色のARは最大、DR／SLは0を基準とする。生成後は通常の音色エディタでAR／DR／SL／RRを調整できる。
- 変換は1回のUndo単位として履歴へ記録し、変換後のGUI、MGSCプレビューおよび試聴音を同期する。
- 入力欄以外で`Ctrl+V`を実行した場合、クリップボードのWAVE音声またはコピーされた`.wav`ファイルを最優先で変換する。SCCではWAVがない場合に画像背景、続いてMGSCテキストを処理し、OPLLではWAVがない場合にMGSCテキストを処理する。
- OPLL変換は任意波形の完全再現ではなく、YM2413の2オペレーターFMで表現可能な範囲の近似音色とする。

PCキーボード演奏と入力欄の競合は、入力欄の種類ごとに制御する。

- 名前、タグ、メモ、検索、読み取り専用テキストおよびSCCの16進入力欄にフォーカスがある間だけPCキーボード演奏を停止する。
- 10進数値入力欄へフォーカスがある場合、数字、符号、編集キーおよびEnterは入力欄へ渡し、`A S D F G H J K`などの演奏割り当てキーは文字入力せず試聴へ使用する。
- OPLLのAR／DR／SL／RR縦バーはWindowsの方向スタイルへ依存せず、論理値と表示位置を明示的に反転し、全項目で0を最下部、15を最上部に配置する。

### 12.2 OPLL単音色エディタ

OPLL単音色エディタは、1つの`@v`に相当するオリジナル音色を編集する。ROM音色は参照専用とし、編集する場合はオリジナル音色へコピーしてから行う。

最低限、次を備える。

- 任意の音色名、タグ
- ROM音色の選択・参照
- ROM音色からオリジナル音色へのコピー
- MOD / CARの全パラメーター
- Feedback
- 単音試聴
- `@v`定義プレビュー

OPLL単音色エディタではMGSC音色番号を扱わない。保存対象は内部ID、任意の音色名、タグおよびMOD / CARの全パラメーターであり、`@v`番号はMGSC出力時に別途割り当てる。

OPLL単音色エディタの右側に、現在指定されている一時出力番号と確定音色から生成したMGSC `@v`定義を読み取り専用で常時表示する。表示にはファイル保存およびクリップボードコピーと同じフォーマッターを使用する。

YM2413の固定音色1～15をプリセットとして一覧表示し、選択した固定音色を編集対象のオリジナル音色へ読み込めるようにする。

- 固定音色パラメーターは`emu2413`のYM2413 ROMパッチ表を取得元とし、別の手書き複製表をGUI側へ持たせない。
- 一覧ではMGSDRV上の`@0`～`@14`と、Violin、Guitar、Pianoなどの固定音色名を併記する。
- 読み込み操作は固定音色そのものを編集するのではなく、MOD／CARの全パラメーターを現在の編集可能なオリジナル音色へコピーする。
- 読み込み直後にチェックボックス、スライダー、数値、MOD／CARエンベロープグラフ、`@v`プレビューおよび試聴音を同期更新する。
- コピー後の音色は通常のオリジナル音色と同じように自由に変更・保存でき、元の固定音色表は変更しない。

内部のOPLL単音色モデルは、MOD／CARそれぞれのAM、PM、EG-TYP、KR、MULTI、KSL、WS、AR、DR、SL、RR、MODのTLおよび共通Feedbackを意味パラメーターとして保持する。YM2413レジスタ0～7とのエンコード／デコードは単一の共通変換を使用し、GUI、試聴、ライブラリ保存およびMGSC `@v`出力が個別にビット配置を実装しない。

MULT、KSL、MOD Total LevelおよびFeedbackの横方向バーは、バーの右側へ現在の整数値を常時表示する。

- MOD／CARのMULTは`0～15`、KSLは`0～3`、MOD TLは`0～63`、FBは`0～7`の実値を10進表示する。
- バーをドラッグして量子化済みの値が変わった時点で数値表示を更新し、ポインター解放まで待たない。
- ROM固定音色、MGSC `@v`、クリップボード、ローカルライブラリ、外部`.mgstc`および編集取消から音色を読み込んだ場合も、意味パラメーター、バー、右側数値、グラフおよび試聴を同期する。
- 右側数値は直接入力欄とし、Enterまたはフォーカス移動で確定する。有効範囲外または不正な値は音色へ反映せず、現在値へ戻す。

MODとCARのハードウェアエンベロープを、同じ時間軸上で識別できるグラフとして表示する。重ね描画または上下に揃えた表示を選択できる構造とし、常にMODとCARの関係を比較できるようにする。

グラフは任意の自由曲線データを保持するものではなく、YM2413の音色パラメーターから計算したエンベロープ曲線である。グラフ上の制御点または区間をドラッグ・スライドすると、対応するYM2413パラメーターを変更する。

- Attack区間の傾き: AR
- Decay区間の傾き: DR
- Sustain位置の高さ: SL
- Release区間の傾き: RR

初期ネイティブ実装ではグラフの横方向を`AR | DR | SL | RR`の4操作区間に分け、押下開始した区間のパラメーターをポインター解放まで保持する。AR・DRは右方向ほど遅く、RRは右方向ほど速く、SLは下方向ほど減衰量が大きくなる操作とし、0～15の整数へスナップする。

UIでは一般的な`A / D / S / R`表記と、YM2413の実パラメーター名`AR / DR / SL / RR`を併記する。各値はYM2413の有効な整数範囲へスナップし、設定不可能な中間値を保持しない。

AR、DR、SL、RRは次の3つの操作方法を双方向同期する。

- グラフ上の制御点・区間のドラッグ
- 数値入力
- 縦方向の上下バー

MOD／CARそれぞれのエンベロープグラフは、4本の縦ガイドが対応するAR／DR／SL／RR数値入力欄の中央と一致する幅および配置にする。

いずれかを変更した時点で、残り2つの表示、MOD / CARグラフ、`@v`定義プレビューおよび試聴音へ即時反映する。

EG-TYP、KSR、Sustain設定および試聴音程など、エンベロープ形状・時間へ影響する項目もグラフへ反映する。時間表示は選択中の試聴音程とチップクロックに基づくプレビューであることを表示する。

エンベロープグラフの波形は、AR／DR／SL／RRから作った一般的なADSRの模式図ではなく、表示専用に分離した`emu2413`インスタンスのエンベロープジェネレーターを進行させて取得する。

- 現在編集中のオリジナル音色レジスタ0～7と、共通`Last Audition Note`の音程を使用する。
- YM2413の内部EG更新周期でキーオンから4秒間をシミュレーションし、1秒地点でキーオフする。横軸へ1秒目盛りとキーオフ位置を表示する。
- MOD／CARそれぞれについて、EG減衰、Total Level、Key Scale LevelおよびAMを含む実効減衰量を`0.375 dB/step`で線形振幅へ変換する。
- MOD／CARは共通の縦軸`0.0～1.0`で表示し、波形ごとのピーク正規化は行わない。これによりTL、KSL、音程およびAMが両オペレーターの相対レベルへ与える影響を比較できる。
- `emu2413`の既存EG状態遷移を再利用しつつ、表示更新では位相・FM波形・ミキシングを生成しない専用進行関数を用いる。表示用シミュレーションは音声出力側のOPLL状態へ影響しない。
- 音色パラメーターまたは最後に試聴したOPLL音程が変化した時点で再計算する。

OPLL単音色エディタに限り、試聴中のFM音源出力波形をリアルタイム表示する。

- 表示元は作図用の疑似波形ではなく、`emu2413`が生成したOPLL単独の実PCMサンプルとする。
- 音源エンジンの48,000Hz出力から800サンプルを1フレームとして取得し、MGSDRVの1カウントと同じ約1/60秒分を表示する。
- 音声スレッドからUIへの受け渡しは固定容量SPSCキューを使用し、音声コールバック内でロック、待機または動的メモリ確保を行わない。
- UIは約16ms間隔でキューを確認し、複数フレームが到着している場合は最新フレームを表示して遅延の蓄積を防ぐ。
- 縦方向は表示専用のピーク自動スケールとし、倍率変更は試聴音、音色パラメーター、ライブラリデータおよびMGSC出力へ影響させない。
- OPLLが発音していない場合は無音の水平線または未取得表示とし、PSG・SCCの出力を混入させない。
- このリアルタイム波形表示はOPLL単音色エディタだけに置き、メイン画面およびSCC単音色エディタの必須表示とはしない。

OPLLリアルタイム波形表示に`周期同期表示`チェックボックスを設け、次の2モードを切り替えられるようにする。

- OFFは`固定時間表示`とし、従来どおり音声ストリームから約1/60秒、800サンプルを時系列のまま表示する。音域ごとの周期数および実際の時間変化を確認するための生波形表示として扱う。
- ONは`周期同期表示`とし、現在の試聴音程から基本周期を求め、音程にかかわらず2周期分がグラフ横幅へ収まるよう表示する。音色パラメーター変更による波形形状の比較にはこのモードを使用する。
- 周期同期表示では、現在音程から求めた予測周期を基準とし、前回表示波形との循環相関および立ち上がり方向のゼロクロスを使用して表示開始位置を決める。ゼロクロスだけに依存せず、FeedbackやMULTによって複数の交差点を持つFM波形でも開始位置が飛びにくいようにする。
- 周期が整数サンプル数にならない場合は、表示専用の線形補間または同等以上の補間によってサブサンプル単位で位置を合わせ、1サンプル未満の位相ずれによる左右の揺れを抑える。
- 低音域では800サンプル内に1周期が収まらないため、音声スレッドからUIへ渡すOPLL単独波形履歴は最低4096サンプルを保持できる固定容量リングバッファとする。画面更新周期はモードにかかわらず約1/60秒を維持する。
- 周期同期処理、補間、表示周期数および開始位置調整は表示専用とし、試聴音、OPLLレジスタ、音色パラメーター、ライブラリデータおよびMGSC出力へ影響させない。
- `周期同期表示`は初期状態をONとする。設定値はアプリケーション設定へ保存し、次回OPLL単音色エディタを開いた時に復元する。
- 手動で表示開始位相を動かす`位相位置`バーは設けない。

### 12.3 SCC単音色エディタ

SCC単音色エディタは、1つの`@s`に相当する32サンプル波形を編集する。

最低限、次を備える。

- 任意の音色名、タグ
- 32サンプル波形の直接描画
- 各サンプルの符号付き10進数および8bit 16進数編集
- グラフと縦軸を揃えた32個の数値入力欄
- マウスでなぞる波形描画
- 基本波形プリセット
- プリセットの倍音倍率バー
- `現在の波形とマージ`チェックボックス
- `平均化`ボタン
- 正規化、反転、平滑化、循環位相移動、基本波形生成
- 単音試聴
- `@s`定義プレビュー

SCCモーフの手動キーフレームまたは確定済み中間フレームを開いた場合も、1つの独立したSCC単音色としてこのウィンドウで編集する。

SCC単音色エディタではMGSC音色番号を扱わない。保存対象は内部ID、任意の音色名、タグおよび32サンプル波形であり、`@s`番号はMGSC出力時に別途割り当てる。

SCC単音色エディタの右側に、現在指定されている一時出力番号と確定済み32サンプルから生成したMGSC `@s`定義を読み取り専用で常時表示する。表示にはファイル保存およびクリップボードコピーと同じフォーマッターを使用する。

- 一時出力番号、音色パラメーター、波形、MGSC／クリップボードインポート、ROMプリセット、ライブラリ読込または編集取消が変化した時点で表示を更新する。
- OPLLの一時出力番号が15～31、SCCが0～31の範囲外または入力途中の場合は、不完全な定義を表示せず有効範囲の案内を表示する。
- 改行、10進意味パラメーターおよびSCCの2桁HEX表記は実際のエクスポート結果と一致させる。
- SCCの非破壊プリセット候補およびA/B候補試聴は確定32サンプルではないため、適用するまで`@s`定義プレビューへ混入させない。

波形グラフのX座標はサンプル0～31、Y座標は符号付き8bit値`-128～127`へ量子化する。ドラッグ中に前回位置から2サンプル以上離れた位置へ移動した場合は、両端値の間を符号付き整数の線形補間で埋め、速いポインター移動でも未変更サンプルの穴を残さない。1回のポインター更新で複数サンプルが変わっても、波形更新と即時発声は1回にまとめる。

32サンプルそれぞれについて、波形グラフのX位置と揃えた上段の符号付き10進入力欄と下段の8bit 16進入力欄を同時に表示する。

- 10進欄は`-128～127`、16進欄は`00～FF`を受け付ける。16進欄は2桁の大文字で表示する。
- 10進欄の`-1`と16進欄の`FF`のように、同じ8bitパターンを符号付き値と符号なし16進値の両方で表現する。
- どちらか一方で有効値を確定すると、反対側の欄、波形グラフ、内部32バイト、Undoおよび即時試聴を同じ1回の変更として更新する。
- 波形のマウス描画、プリセット、マージ、平均化、正規化、反転、位相移動、MGSCインポート、ライブラリ読込および編集取消で値が変化した場合も、両方の数値欄を同期する。
- 空欄、不完全な16進値、10進範囲外または`FF`を超える入力は音色へ反映せず、フォーカスを離した時点で現在の有効値へ表示を戻す。不正値ではUndoと即時発声を発生させない。
- 現在値と同じ値を別表記から確定した場合は表示だけを正規化し、重複するUndoおよび即時発声を行わない。

波形グラフの背面に、既存画像に描かれた波形を手作業でトレースするための任意背景画像を配置できるようにする。

- Windows Imaging ComponentでPNG、JPEG、BMP、GIFおよびTIFFを読み込む。
- 画像は波形グラフの描画範囲内へクリップし、波形線とグリッドより背面へ描画する。
- 配置X、配置Y、表示幅および表示高さをピクセル単位の整数で直接指定できる。
- 配置X、配置Y、表示幅および表示高さは数値入力に加えて横方向バーでも調整でき、どちらを操作しても相互に同期する。
- 表示幅と表示高さは独立して変更でき、元画像の縦横比を強制しない。
- 表示ON / OFFおよび不透明度を変更できる。
- 背景画像の操作ではSCCの32サンプル値を変更せず、即時発声を行わない。
- 背景画像のファイル参照、配置、寸法、表示状態および不透明度はエディタ／プロジェクト補助情報として扱い、単音色ライブラリ本体、`@s`定義およびMGSC出力へ含めない。
- 参照先画像が存在しない場合も音色データを失わず、背景だけを未読込として警告できる構造にする。
- 初期ネイティブ実装では、背景画像パス、X／Y、表示幅／高さ、不透明度および表示ON/OFFを`settings-v1.ini`の版付き`SccBackground`セクションへ保存する。
- 新しい画像を選択した時、位置・寸法・不透明度・表示状態を変更した時、およびSCC単音色エディタを閉じる時に補助設定を更新する。
- 次回SCC単音色エディタ生成時に補助設定を読み込み、Windows Imaging Componentで参照画像を再読込して全コントロールへ反映する。
- 保存パスが空の場合は背景なしで起動する。保存パスが存在しても画像を開けない場合は、パスと配置設定を保持したまま背景ビットマップだけ未読込とし、画面状態欄で通知する。
- 背景復元の成否、設定保存および参照画像欠落は、SCCの32サンプル、Undo、即時試聴、ライブラリおよびMGSC定義を変更しない。
- SCC単音色エディタで`Ctrl+V`を実行した際、クリップボードに画像がある場合はMGSCテキストより優先し、その画像を波形グラフ背面の背景画像として読み込む。画像貼り付けでは32サンプルを変更しない。
- 基本波形プリセット一覧は名称だけでなく、各プリセットの1周期波形を示す小型プレビュー画像を併記する。

### 12.4 単音色編集時の即時発声

OPLL単音色エディタとSCC単音色エディタに、パラメーター変更結果を直ちに単音で確認する`即時発声`機能を設ける。

- OPLLでは、音色に関係する有効なパラメーター値が変更されるたびに、変更後の音色で発声する。
- AR / DR / SL / RRの縦方向バーおよびグラフ上の制御点をドラッグ中は、マウスまたはポインターを離すまで待たず、有効値が変わったタイミングごとに発声する。
- 数値入力では、入力値が有効範囲内の確定値になったタイミングで発声する。空欄や入力途中の不正値では発声しない。
- SCCでは、32サンプルの数値編集、波形の直接描画、正規化、反転、平滑化、循環位相移動、基本波形生成などによって有効な波形が変更されるたびに、変更後の波形で発声する。
- SCC波形をドラッグ描画中も、ポインターを離すまで待たず、有効なサンプル値が変わったタイミングごとに発声する。
- 同一の量子化済み値の範囲内でポインターだけが移動し、実際の音色データが変化していない場合は重複発声しない。
- 複数パラメーターを一括変更する1回の操作は、変更完了後の音色で1回発声する。
- Undo / Redoで音色が変化した場合も、操作後の音色で1回発声する。

即時発声に使用する音階は、ユーザーが最後にPCキーボードまたは画面上の発声ボタンで手動発声した音階とする。この音階をアプリケーションセッション内の共通`Last Audition Note`として記憶し、OPLLとSCCの両単音色エディタで共有する。

- 手動発声を行うたびに`Last Audition Note`を更新する。
- パラメーター変更による自動発声では`Last Audition Note`を変更しない。
- セッション開始後に一度も手動発声していない場合はC4を使用する。
- `Last Audition Note`は音色データおよびMGSC出力には保存しない。

即時発声では、既に鳴っている即時試聴音があれば停止し、変更後の音色を同じ`Last Audition Note`で再キーオンする。これにより、OPLLのAttackを含むハードウェアエンベロープの変更も毎回先頭から確認できる。パラメーター変更による自動試聴は再発声から1秒後に自動キーオフする。変更が連続した場合は最後の再発声から1秒を数え直す。この自動キーオフはPCキー、画面鍵盤およびMIDI入力による手動演奏には適用しない。

各単音色エディタに`即時発声`チェックボックスを表示する。

- ON: 上記のパラメーター変更時発声を行う。
- OFF: パラメーター変更時の自動発声だけを停止する。
- OFFでもPCキーボードおよび発声ボタンによる手動試聴は利用できる。
- OPLL用とSCC用のON / OFF状態を音源別に記憶する。
- ON / OFF状態はアプリケーション設定であり、単音色データおよびMGSC出力には含めない。

初期ネイティブ実装では、OPLL／SCCそれぞれの`即時発声`ON/OFFとPC演奏オクターブを`%LOCALAPPDATA%\MgsToneCraft\settings-v1.ini`へ保存する。

- 設定ファイルにスキーマバージョンを持たせ、未対応版は既定値で起動する。
- 即時発声チェック変更時、PC演奏オクターブ変更時および通常終了時に保存する。
- 次回起動時は単音色エディタ生成前に読み込み、チェック状態とPCキー割り当てへ反映する。
- 設定値は音色ライブラリ、外部`.mgstc`およびMGSC出力へ含めない。

試聴は画面上の演奏ボタンとグラフィカルなピアノ鍵盤に加え、PCキーボードでも行えるようにする。画面鍵盤は白鍵と黒鍵を実際の鍵盤と同じ位置関係で表示する。

- 画面鍵盤には共通発音範囲`C1`～`B8`の8オクターブを同時表示する。
- 画面鍵盤はマウスの押下で発音を開始し、離したときにキーオフする。ペン固有のWindows Pointer入力対応は要件としない。
- マウスアップ、ポインターキャプチャ喪失、フォーカス喪失および操作キャンセルのいずれでもキーオフする。通知を取りこぼした場合に備え、マウス試聴中は左ボタンの実状態を監視し、既に離されていれば強制キーオフする。
- 左ボタン解放の監視で異常を検出した場合は、音源へのキーオフだけでなく、画面鍵盤の保持ノート、フォーカスおよびマウスキャプチャ状態も同時に解除する。
- ポインターを押したまま複数の鍵盤上を左右になぞると、通過した鍵ごとに発音音階を切り替え、連続して演奏できる。
- ドラッグ中に同じ鍵の範囲内だけを移動した場合は、同じ音階を重複して再キーオンしない。
- 黒鍵も画面上でクリックおよびドラッグ演奏できる。

PCキーボードの白鍵標準割り当ては次とする。

```text
A S D F G H J K
C D E F G A B >C
```

半音は白鍵の上段にあたるPCキーへ、ピアノ鍵盤と同じ欠番位置を保って割り当てる。

```text
 W E   T Y U
C# D# F# G# A#
```

したがって標準配列は、`A/W/S/E/D/F/T/G/Y/H/U/J/K`の順で
`C/C#/D/D#/E/F/F#/G/G#/A/A#/B/>C`へ対応する。

PCキーの演奏オクターブは`Z`で1オクターブ下降、`X`で1オクターブ上昇し、`C1`～`B8`の範囲を移動できる。現在のPC演奏オクターブとキー割り当てを画面鍵盤上へ表示する。オクターブ変更時は発音中のキーを先にキーオフする。

入力欄へフォーカスがある場合は、文字入力を優先して試聴キーを発音させない。

### 12.5 MIDIキーボード入力

- WindowsのMIDI入力デバイスから`Note On`、`Note Off`およびVelocityを受信し、画面鍵盤・PCキーと同じ試聴経路へ入力する。
- MIDIは演奏入力に使用し、本アプリからMIDI音源へ演奏データを出力する機能とは区別する。
- 初期実装は広いWindows互換性を優先してWinMM MIDI Inputを使用する。
- 入力デバイスを選択でき、切断・再接続を扱えること。
- 受信チャンネルはOmniまたは1～16から選択できること。
- `Note On`のVelocity 0は`Note Off`として扱う。
- デバイス切断、切替、停止時にはAll Notes Off相当の処理を行い、発音を残さないこと。
- 初期の単音試聴はモノフォニックとし、Velocityの音量反映はON / OFFを選択可能にする。

## 13. 内部データ設計方針

- 内部データはMGSC出力文字列だけで保持しない。
- 音色、レイヤー、キーフレーム、エンベロープ命令、オートメーションを構造化データとして保持する。
- OPLLパッチスライドとOriginal Tone Busオートメーションは独立データとして保持し、共通時間軸上で決定的にマージする。
- SCCプリセットマージとSCCモーフ自動補間は、共通の波形ブレンド実装を使用する。
- SCCモーフには、補間カーブ、位相自動調整の有効状態、循環シフト量、極性、RMS補間条件および手動指定の有無を区間ごとに保存する。
- 自動生成されたSCC派生フレームは元の単音色IDと生成条件を保持し、元のライブラリ単音色を変更せず再生成できるようにする。
- `@e`は命令の順序を保持するトークン列またはASTとして管理する。
- GUI編集とMGSCテキストの双方向変換を可能にする設計とする。
- MGSCへ出力できない内部状態を作った場合、出力時ではなく編集時点で警告する。
- 保存形式にはスキーマバージョンを付与する。
- 単音色エディタの`Last Audition Note`と音源別の`即時発声`ON / OFF状態はアプリケーションセッションまたは設定として保持し、音色ライブラリ項目には含めない。

## 14. 初期受け入れ条件

初期版の主要機能は、少なくとも次の条件を満たしたとき完成とみなす。

1. YM2149、SCC、YM2413を単独で試聴できる。
2. 3音源を含む複数レイヤーを同期試聴できる。
3. 名前付き複合音色を複数保存・再読み込みできる。
4. SCCの32サンプル波形を編集できる。
5. SCCの手動キーフレームと自動補間フレームを混在させられる。
6. SCCモーフを中間`@s`群と`@e`音色切り替えとして出力できる。
7. OPLLのキーオンを維持したままROM音色・オリジナル音色を切り替えるパッチスライドを作成・試聴・出力できる。
8. パッチ音色イベントが適用カウントと音色番号だけで成立し、キーオン指定および保持カウントを要求しない。
9. OPLLのMOD Total LevelとFeedbackを個別に有効・無効化し、時間変化を`y`命令として出力できる。
10. 同一OPLLレイヤーでパッチスライドと`y`レジスタオートメーションを同時に使用できる。
11. 同一カウントの音色切り替えと`y`書き込みが、音色選択、レジスタ書き込み、音量命令による時間進行の順で出力される。
12. SSG・SCC・OPLLでMGSDRVソフトウェアエンベロープを利用でき、三音源のレーンが約1/60秒固定の同一カウント軸で表示される。
13. ソフトウェア音量を`0`～`f`で編集・表示・出力できる。
14. OPLLおよびSCCの単音色を、それぞれモデルレスの専用ウィンドウで編集できる。
15. 単音色データがソフトウェアエンベロープ、パッチスライド、レジスタオートメーション、モーフシーケンスおよび音長を含まない。
16. OPLL単音色エディタでMOD / CARのエンベロープをグラフィカルに比較でき、AR / DR / SL / RRをグラフ、数値、縦バーのいずれからでも編集できる。
17. グラフ、数値、縦バーのいずれかを変更すると、他の表示、`@v`プレビューおよび試聴音が同期更新される。
18. SCC単音色エディタで32サンプル全値を編集・単音試聴できる。
19. SCC波形グラフと32個の数値入力欄が同じサンプル位置の縦軸で揃い、数値を直接入力できる。
20. SCC波形をマウスでなぞって描画でき、飛び越えたサンプル位置が補間される。
21. 基本波形プリセットを選び、`1.0x`、`1.5x`、`2.0x`を含む倍音倍率を変更できる。
22. `1.5x`が非整数周期ではなく、位相を揃えた`1.0x`と`2.0x`の50%倍音ブレンドとして生成される。
23. プリセット適用時に、現在の波形を置換するかマージするかをチェックボックスで選択できる。
24. プリセットマージでDC除去、循環位相合わせ、任意の極性反転、RMS音量合わせ、リニア補間、全体スケーリングおよび8bit量子化が順序どおり行われる。
25. SCCモーフの自動中間波形でも同じ補間基盤を使用し、区間の開始RMSから終了RMSまで滑らかに補間する。
26. 複数の手動キーフレームを順番に位相整合し、元のライブラリ単音色を変更せず派生フレームだけを生成する。
27. 往復モーフが同じフレームを逆順に使用し、ループモーフが末尾と先頭の接続も含めて位相整合を行う。
28. `平均化`ボタンで循環3サンプル移動平均を適用し、Undoできる。
29. OPLL・SCC単音色を任意の名前で保存でき、単音色エディタではMGSC音色番号を扱わない。
30. 同じOPLL・SCCライブラリ音色を、MGSC出力ごとに有効範囲内の任意の音色番号へ割り当てられる。
31. 画面上の演奏操作とPCキーボードの`A S D F G H J K`で`C D E F G A B >C`を試聴できる。
32. OPLLおよびSCC単音色エディタで`即時発声`をONにすると、有効な音色パラメーターが変化したタイミングごとに変更後の音で発声する。
33. 縦バー、グラフおよびSCC波形のドラッグ中に、ポインターを離す前でも有効値が変わるたびに発声する。
34. 即時発声が最後に手動発声した音階を使用し、自動発声自体では記憶音階を変更しない。
35. `即時発声`をOFFにしてもPCキーボードおよび発声ボタンによる手動試聴を利用できる。
36. 各音源で初期実装対象の全音色パラメーターを編集できる。
37. MGSC 1.11でコンパイル可能なShift-JIS / CRLFの`.MUS`を出力できる。
38. 主要な番号・文字数・共有資源競合を検出できる。
39. GUI試聴へ渡したレジスタイベントとMGSC出力のイベント内容が一致する。
40. OPLLまたはSCC単音色エディタを開いた状態でもメイン画面を操作でき、単音色パラメーターとメイン画面のソフトウェアエンベロープを交互に編集・試聴できる。
41. 画面上に白鍵と黒鍵を実配置したグラフィカル鍵盤を表示し、各鍵を直接クリックして試聴できる。
42. 画面鍵盤上をポインター押下状態のまま横断すると、通過した鍵ごとに発音音階が切り替わる。
43. PCキーボードの`A S D F G H J K`で白鍵、`W E T Y U`で対応する黒鍵を試聴できる。
44. 初期状態の音量バランスがPSG:SCC:OPLL = `1.0:1.0:1.0`、マスターゲイン = `3.0`であり、RMS均一化を行わない。
45. UI→音声および音声→UIのコマンド配送が固定容量SPSCキューで行われ、音声コールバック内でロックまたはキュー用動的メモリ確保を行わない。
46. 不変プログラムが事前確保スロットと世代番号で交換され、音声スレッド上で音源生成・破棄やヒープ確保をせず、切替と同じコールバック内で指定音階を再トリガーできる。
47. Windows共有モード・イベント駆動WASAPIが48,000Hz float stereoを受け取り、現在のパディング分だけを補充し、開始・停止・アンダーラン・デバイスエラーをUIへ通知できる。
48. 追加GUIランタイムなしのWin32ネイティブEXEをビルドでき、固定メイン画面、モデルレスOPLL・SCC別窓、画面鍵盤の押下ドラッグ、PCキーの白鍵・黒鍵および停止操作が同一リアルタイム音源へ接続される。
49. ネイティブ画面でPSG・SCC・OPLLの試聴先を切り替えられ、状態表示にMIDI発音と誤認させる番号を出さず、`C4`等の音名を表示する。
50. 画面鍵盤に`C1`～`B8`の8オクターブを同時表示し、全鍵をクリックおよびドラッグ試聴できる。
51. `Z`／`X`でPC演奏オクターブを変更でき、現在の`A/W/S/E/D/F/T/G/Y/H/U/J/K`割り当てが8オクターブ鍵盤上に表示される。
52. 選択したWindows MIDI入力デバイスからNote On／Offを受信し、チャンネル条件、Velocity 0および切断時All Notes Offを含めて同じ試聴経路で処理できる。
53. OPLL・SCCの音色パラメーター変更による自動試聴が、最後の変更から1秒後にキーオフし、手動演奏を誤って停止しない。
54. 画面鍵盤のマウスアップ通知が失われた場合も左ボタンの実状態から100ms以内に検出し、発音を停止できる。
55. OPLL単音色画面が生レジスタ編集を標準UIにせず、MOD／CAR別の意味パラメーター、AR／DR／SL／RRの縦バーと数値、および両エンベロープのグラフを表示して試聴へ反映できる。
56. MOD／CARエンベロープグラフのAR／DR／SL／RR区間を直接ドラッグすると、対応する整数値、縦バー、数値欄、グラフおよび即時試聴が操作中に同期する。
57. SCC波形グラフをマウスでなぞって32サンプルを直接変更でき、飛び越したサンプルを線形補間し、数値欄・グラフ・即時試聴を同期できる。
58. SCC波形グラフの背面へ画像を読み込み、X／Y／幅／高さをピクセル単位で独立調整し、表示状態と不透明度を変更しても音色値およびMGSC出力が変化しない。
59. OPLL単音色エディタが`emu2413`のOPLL単独PCMを800サンプル単位で受け取り、約1/60秒間隔で最新波形を表示し、表示用自動スケールが試聴音および音色データへ影響しない。
60. OPLL単音色エディタでYM2413固定音色1～15を`@0`～`@14`のプリセットとして選択し、`emu2413`のROMパッチ値を編集可能なオリジナル音色へ読み込むと、全GUI表示と試聴音が同期更新される。
61. OPLL単音色エディタで曲全体または定義単体のMGSC `@v`をインポートし、任意の出力番号15～31を指定した`@v`定義をCRLFテキストとしてエクスポートできる。
62. SCC単音色エディタで曲全体または定義単体のMGSC `@s`をインポートし、任意の出力番号0～31を指定した32バイトの`@s`定義をCRLFテキストとしてエクスポートできる。
63. OPLL／SCC単音色エディタで、ファイル入出力と同じMGSC `@v`／`@s`定義をクリップボードへコピーおよび貼り付けでき、不正なクリップボード内容では編集中音色が変化しない。
64. OPLLのMOD／CARエンベロープグラフが、現在音色と最後の試聴音程を使用した`emu2413`のEG状態遷移から生成され、4秒の共通時間軸、1秒地点のキーオフ、TL／KSL／AMを含む正規化しない共通振幅軸を表示する。
65. OPLL／SCC単音色をMGSC音色番号とは独立した64bit内部IDと任意名で複数保存でき、タグ、メモ、お気に入り、作成／更新日時および形式版を含むローカルライブラリをアプリ再起動後に再読込し、名前・タグで絞り込める。
66. 選択した単音色を外部`.mgstc`へ1件だけエクスポートして再インポートでき、同カテゴリに同名音色が存在する場合も既存項目を変更せず、連番付きの名前と新しい内部IDで追加される。
67. OPLL／SCC単音色エディタで`変更を取消`を実行すると、直前の読込・新規・適用・別名保存時点の音色データ、名前、タグ、メモ、お気に入りおよび内部IDへ戻り、復元後の音を即時試聴できる。
68. SCCの全32サンプルを符号付き10進数と2桁8bit 16進数のどちらからでも編集でき、一方の確定時に他方、波形、Undoおよび試聴が同期し、不正入力では音色が変化しない。
69. OPLL単音色エディタのMOD／CAR MULT、KSL、MOD TLおよびFBの各横バー右側に有効範囲内の現在値が表示され、バー操作と全音色読込経路で即時同期する。
70. SCCプリセット種類、3位置の倍音倍率バーおよび全マージ条件を変更すると、確定波形を保持したまま候補が重ね表示・即時試聴され、適用で1回のUndo単位として確定し、取消では元波形が変化しない。
71. SCCプリセット候補がある間、A/B試聴で確定波形と候補波形を同じ音程により交互に発声でき、即時発声OFFでも手動比較でき、候補破棄後は操作できない。
72. OPLL／SCC単音色エディタに確定音色のMGSC `@v`／`@s`定義が常時表示され、出力番号と全編集・読込経路へ同期し、SCCの未適用候補は表示内容を変更しない。
73. OPLL／SCC単音色エディタの発声ボタンが共通`Last Audition Note`を表示し、即時発声OFFでも確定音色を1秒間手動試聴できる。
74. OPLL／SCCの即時発声ON/OFFとPC演奏オクターブが版付きアプリ設定へ保存され、再起動後に復元される。
75. SCC背景画像のパス、位置、寸法、不透明度および表示状態が音色データと分離して保存・復元され、参照画像が欠落してもSCC波形を変更せず背景だけ未読込として通知する。

## 15. 廃止・置換された案

次の案は現在の仕様では採用しない。

- MAMEのYM2149実装を製品へ組み込む案  
  → `emu2149`へ置換。
- MAMEのSCC実装を製品へ組み込む案  
  → `emu2212`へ置換。
- SSGソフトウェアエンベロープをSSGパネルだけに置く案  
  → 3音源共通のエンベロープ領域へ置換。
- SCCモーフをホスト側だけの連続補間エフェクトとして再生する案  
  → 中間`@s`波形を生成し、MGSDRVの`@e`で切り替える方式へ置換。
- MGSCの待ち時間を`f4`と出力する案  
  → 正しい`f:4`形式へ訂正。

## 16. 未決事項

以下は現時点では確定しておらず、実装開始時または各機能の詳細設計時に決定する。

- アプリケーションのUIフレームワークと配布形態
- ライブラリ保存ファイルの具体的な形式
- SCC+対応の範囲
- MGSCテキストからの完全な再インポート範囲
- OPLLの全パラメーターを初期版でオートメーション可能にするか
- 複数音色を一括して1曲用の番号空間へ割り当てる詳細UI
- 実機との音声比較テスト環境

## 17. 参照資料

- MGSDRV API仕様  
  https://gigamix.hatenablog.com/entry/mgsdrv/api-specifications
- MGSC 1.11ユーザーマニュアル  
  https://www.gigamix.jp/mgsdrv/MGSC111.TXT
- MGSDRVテクニック・資料集  
  https://gigamix.hatenablog.com/entry/mgsdrv/technique
- MGSDRV v3.xxデータ形式解析  
  https://github.com/digital-sound-antiques/mgsc/blob/main/mgs-format.md
- MGSDRV 3.20エンベロープ動作解析  
  `analysis/MGSDRV_ENVELOPE_ANALYSIS.md`
- YM2413アプリケーションマニュアル  
  https://d4.princess.ne.jp/msx/datas/OPLL/YM2413AP.html
- emu2149  
  https://github.com/digital-sound-antiques/emu2149
- emu2212  
  https://github.com/digital-sound-antiques/emu2212
- emu2413  
  https://github.com/digital-sound-antiques/emu2413
- Wavetable Synthesizer algorithm  
  https://www.mathworks.com/help/audio/ref/wavetablesynthesizer-system-object.html
- SciPy cross-correlation lag reference  
  https://docs.scipy.org/doc/scipy-1.11.1/reference/generated/scipy.signal.correlation_lags.html
- SciPy DC / constant detrend reference  
  https://docs.scipy.org/doc/scipy-1.14.0/reference/generated/scipy.signal.detrend.html
- NumPy DFT reference  
  https://numpy.org/doc/2.2/reference/routines.fft.html
- A Data-Driven Approach to Wavetable-Synthesis  
  https://www.creative-technologies.de/a-data-driven-approach-to-wavetable-synthesis/

## 18. 変更履歴

| 文書版 | 日付 | 内容 |
|---|---|---|
| 0.58 | 2026-07-28 | 正式製品名を「MGS Tone Craft」、略称と内部識別子を`MGSTC`／`mgstc`、GitHubリポジトリ名を`mgs-tone-craft`、実行ファイル名を`mgstc.exe`に決定。名前空間、ヘッダーパス、ビルドターゲット、表示名、保存先、保存形式IDおよび外部音色拡張子`.mgstc`を統一 |
| 0.57 | 2026-07-26 | OPLLリアルタイム波形へ`周期同期表示`チェックボックスを追加。OFFの800サンプル固定時間表示、ONの2周期・音程基準・前フレーム相関・ゼロクロス・サブサンプル補間による同期表示、低音対応4096サンプル履歴、約1/60秒更新、設定永続化、表示専用処理、および手動位相位置バーを設けない方針を固定 |
| 0.56 | 2026-07-26 | OPLL WAV近似を単一局所スペクトル探索から、倍音＋循環位相・極性整合波形の複合評価、ROM／広域候補、上位複数初期点の多段探索へ変更。PC演奏停止を文字・HEX欄へ限定し、10進数値欄フォーカス中の演奏を復旧。OPLL AR／DR／SL／RR縦バーを論理値から明示反転して0最下部・15最上部へ固定 |
| 0.55 | 2026-07-26 | OPLL／SCC単音色エディタへ`WAV変換`を追加。PCM／float／extensible WAVE読込、モノラル化、単周期または周期検出・最大16周期平均、DC除去・正規化、SCC 32サンプル変換、emu2413倍音スペクトル比較によるOPLL近似探索、Undo・試聴同期、およびCtrl+VのWAV音声／`.wav`ファイル優先取込を固定 |
| 0.54 | 2026-07-26 | 数値欄のEnter確定、256段Undo／RedoとCtrlショートカット、文字入力中のPC演奏停止、Tab移動、縦バーの0最下部を共通化。音色Ctrl+C/V、エディタ表示時の試聴音源切替、SCC画像貼り付け・背景調整バー・プリセット波形サムネイル、OPLL MULT／KSL／TL／FB直接入力およびEGグラフと数値列の軸合わせを追加 |
| 0.53 | 2026-07-26 | SCC背景画像の補助設定を版付きアプリ設定へ永続化。画像パス、X／Y、幅／高さ、不透明度、表示状態の保存・復元、WIC再読込、参照欠落時の非破壊通知および音色・MGSC・Undoからの分離を固定 |
| 0.52 | 2026-07-26 | ペン固有入力対応を要件から除外し、マウス操作へ確定。OPLL／SCC単音色エディタへLast Audition Note付き1秒発声ボタンを追加し、音源別即時発声設定とPC演奏オクターブの版付きINI永続化を固定 |
| 0.51 | 2026-07-26 | OPLL／SCC単音色エディタへ確定音色のMGSC `@v`／`@s`ライブプレビューを追加。エクスポート共通フォーマッター、出力番号・全編集経路との同期、無効番号案内、およびSCC未適用候補との分離を固定 |
| 0.50 | 2026-07-26 | SCCプリセットプレビューへA/B交互試聴を追加。A確定波形／B候補波形の同音程再発声、次試聴側表示、明示操作として即時発声OFF時も利用可能、1秒キーオフおよび候補破棄時の無効化を固定 |
| 0.49 | 2026-07-26 | SCCプリセット／マージの非破壊ライブプレビューを追加。確定波形と候補の重ね表示、3位置の倍音倍率バー、全条件変更時の再計算・即時試聴、適用時の単一Undo、取消時の原音復帰および確定波形変更時の候補破棄を固定 |
| 0.48 | 2026-07-26 | OPLL単音色エディタのMULT／KSL／MOD TL／FB横バー右側へ現在値表示を追加。ドラッグ中の即時更新、各有効範囲、およびROM・MGSC・ライブラリ・取消を含む全読込経路との同期を固定 |
| 0.47 | 2026-07-26 | SCCの32サンプルへ符号付きDEC／8bit HEXの2段入力を追加。波形処理を含む双方向同期、2桁大文字HEX、同値時の重複発声抑止、不正入力の非反映と現在値復帰、および有効変更単位のUndoを固定 |
| 0.46 | 2026-07-26 | 単音色エディタへ編集基準点と`変更を取消`を追加。読込・新規・適用・別名保存を基準点とし、OPLL／SCC音色データ、名前、タグ、メモ、お気に入り、内部ID、GUIおよび試聴状態を一括復元する動作を固定 |
| 0.45 | 2026-07-26 | 単音色ライブラリの外部音色ファイルのインポートと選択項目エクスポートを追加。複数項目取込、新規内部ID・日時の割当、同カテゴリ同名時の`(1)`連番、既存項目非上書き、別カテゴリ同名の許可および不正ファイルの全件拒否を固定 |
| 0.44 | 2026-07-26 | OPLL／SCC単音色のローカルライブラリを追加。64bit内部ID、名前、タグ、メモ、お気に入り、作成／更新日時、形式版、カテゴリ別一覧、名前・タグ検索、新規・読込・保存・別名保存・削除、バージョン付き永続形式および一時ファイルからの安全な置換保存を固定 |
| 0.43 | 2026-07-26 | OPLLのMOD／CARグラフを模式的ADSRから`emu2413`の実EG状態遷移による表示へ変更。現在音色・最後の試聴音程、4秒時間軸、1秒キーオフ、TL／KSL／AMを含む実効レベル、非正規化の共通振幅軸および音声状態から分離した高速EG進行を固定 |
| 0.42 | 2026-07-26 | OPLL／SCC単音色エディタへMGSC `@v`／`@s`定義のクリップボードコピー・貼り付けを追加。Unicode／ANSI両形式のコピー、Unicode優先貼り付け、およびファイル入出力と共通の検証・番号分離を固定 |
| 0.41 | 2026-07-26 | OPLL／SCC単音色エディタへMGSC `@v`／`@s`のファイルインポート・エクスポートを追加。曲全体からの定義抽出、コメント除外、個数・範囲検証、定義番号と音色データの分離、出力時番号指定、CP932互換ASCII／CRLF出力を固定 |
| 0.40 | 2026-07-26 | OPLL単音色エディタへYM2413固定音色1～15のプリセット一覧と編集音色への読込を追加。`emu2413`のROMパッチ表を単一の取得元とし、MGSDRV `@0`～`@14`表記、全意味パラメーター・グラフ・試聴の同期を固定 |
| 0.39 | 2026-07-26 | SCC単音色エディタへSine／Square／Triangle／Saw Up／Saw Down／Pulse、1.0x／1.5x／2.0x倍音、位相・極性・RMSを考慮したプリセットマージ、循環3サンプル平均化、正規化、反転、循環位相移動およびUndoを追加。波形処理を将来のSCCモーフでも再利用する共通エンジンとして実装 |
| 0.38 | 2026-07-26 | OPLL単音色エディタへ`emu2413`の実PCM波形を800サンプル／約1/60秒単位で表示する機能を追加。固定容量SPSCキュー、最新フレーム優先、OPLL単独信号および表示専用自動スケールを固定 |
| 0.37 | 2026-07-25 | SCC波形を手動トレースする背景画像機能を追加。WIC画像読込、グラフ内背面描画、X／Y／幅／高さのピクセル指定、自由な縦横比、不透明度、表示切替、および音色・MGSC出力からの分離を固定 |
| 0.36 | 2026-07-25 | SCC暫定縦バー画面を32サンプル波形グラフへ置換し、マウス直接描画、飛び越したサンプルの符号付き線形補間、数値欄・グラフ・即時試聴の同期を追加 |
| 0.35 | 2026-07-25 | MOD／CARエンベロープグラフをAR・DR・SL・RRの4操作区間に分け、直接ドラッグで整数値、縦バー、数値、グラフおよび即時試聴を同期する操作を追加 |
| 0.34 | 2026-07-25 | OPLL暫定生レジスタ画面をMOD／CAR別の意味パラメーター画面へ置換し、AM・PM・EG・KR・MULT・KSL・TL・FB・WS、AR／DR／SL／RRの縦バー・数値、両エンベローププレビューを即時試聴へ接続 |
| 0.33 | 2026-07-25 | OPLLオリジナル音色の全意味パラメーターとYM2413レジスタ0～7を相互変換する共通モデルを追加し、GUI・試聴・保存・MGSC出力で変換を共有する方針を固定 |
| 0.32 | 2026-07-25 | 開発初期の仮称を正式リリース前に独自の製品名へ変更し、アプリ表示名・EXE・ビルドターゲット・GitHub・文書・配布物を統一する要件を追加 |
| 0.31 | 2026-07-25 | 起動直後の初期OPLL音色でキーオフ後も音が残ることを防ぐため、MOD／CARの初期RRを0から15へ変更。画面鍵盤の異常解放時に保持ノートとマウスキャプチャも強制解除する処理を追加 |
| 0.30 | 2026-07-25 | OPLL・SCC音色パラメーター変更による即時発声を最後の変更から1秒後に自動キーオフする仕様へ変更。画面鍵盤のマウスアップ取りこぼし対策として、キャプチャ／フォーカス喪失・操作キャンセル時の停止と左ボタン実状態による安全キーオフを追加 |
| 0.29 | 2026-07-25 | 画面鍵盤を共通発音範囲C1～B8の8オクターブ同時表示とし、PC演奏オクターブをZ／Xで移動する仕様を追加。WinMMによるMIDIキーボード入力、デバイス・チャンネル選択、Velocity 0、切断時All Notes Offを追加。未キーオンのプログラムスナップショットはエンベロープ実行を開始しないことを固定 |
| 0.28 | 2026-07-25 | ネイティブ単音色エディタの初期実装として、SCC 32サンプル値とOPLLレジスタ00～07の数値／バー編集、音源別即時発声、最終手動音程での再トリガーを音源エンジンへ接続。PSGのシーケンス型ソフトウェアエンベロープはキーオフ時に音量0となり、再キーオンまでループ出力を再開しないことを明記 |
| 0.27 | 2026-07-25 | ネイティブ画面にPSG・SCC・OPLL試聴切替を追加し、内部音高番号はMIDI発音を意味しないこと、UIではC4等の音名を表示することを明記 |
| 0.26 | 2026-07-25 | UI基盤を追加ランタイム不要のWin32 API＋標準／カスタムコントロールへ確定し、固定メイン画面、モデルレスOPLL・SCC別窓、押下ドラッグ鍵盤、PCキー白鍵・黒鍵、停止操作をWASAPI音源へ接続 |
| 0.25 | 2026-07-25 | Windows共有モード・イベント駆動WASAPI AudioSink、48kHz float stereo、自動PCM変換、パディング基準のバッファ補充、開始・停止・アンダーラン・デバイスエラー通知を追加 |
| 0.24 | 2026-07-25 | 3スロット・世代番号付き不変プログラムプール、Free/Editing/Pending/Activeの所有権移譲、音声スレッドで確保・解放しないスナップショット交換、同一コールバック内の即時再発声とProgramActivated通知を追加 |
| 0.23 | 2026-07-25 | Phase 3の固定容量SPSCコマンド／通知キュー、音声コールバック先頭でのNoteOn・NoteOff・Stop・HardReset・ゲイン変更、不正コマンド・レンダー失敗・クリップ状態通知を追加 |
| 0.22 | 2026-07-25 | 既定ミキサーバランスをmsxplay-js 1.9.1 / libkss-js 2.2.1基準のPSG:SCC:OPLL = 1:1:1、マスターゲイン3.0として固定し、自動RMS均一化を行わない方針を追加 |
| 0.21 | 2026-07-25 | 固定基準サイズの簡潔・高密度な画面方針、OPLL/SCC単音色エディタ表示中もメイン操作を無効化しないモデルレス並行編集、白鍵・黒鍵を実配置したグラフィカル鍵盤、押下ドラッグによる連続演奏、およびPC上段`W E T Y U`の半音割り当てを追加 |
| 0.20 | 2026-07-25 | `ENGINE_DESIGN.md`を追加し、UI非依存C++20コア、48kHz/800サンプル固定カウント、MGSDRV意味実行層とチップアダプタの分離、各音源1チップ、事前確保イベント列およびリアルタイム安全なスナップショット交換を確定 |
| 0.19 | 2026-07-25 | 多チャンネル・大量レジスタ書き込み時に実機MGSDRVの処理が1/60秒枠を越えて後段へずれ込む現象は再現対象外とし、同一カウント内の順序だけを保持した理想固定約1/60秒タイムラインを採用 |
| 0.18 | 2026-07-25 | `TEST_RHYTHM9.MGS/VGM`の照合に基づき、F先頭の切替前後60カウントガード、`y14,32`とBass Drum用`y14,48`、Ch.7～9共有音量ニブル、および`@0～14`から物理減衰1～15への1ずれ変換を固定。「@15相当の消音」という誤記を最大音量へ訂正 |
| 0.17 | 2026-07-25 | 9音リズムモードへの`y14,32`切替をFチャンネル先頭の原則1回に限定し、ソフトウェアエンベロープ内への配置を禁止、意図しない打楽器発音を防ぐ前後の`v0r2`無音ガードを必須化 |
| 0.16 | 2026-07-25 | `TEST_AUTOMATION.MGS/VGM`の照合に基づき、OPLLオリジナル音色切替時のレジスタ0～7ロード・一時消音・`y`・現在音量再出力の順序と、SCCの32バイト波形転送後の音量出力を固定 |
| 0.15 | 2026-07-25 | `TEST_RATE.MGS/VGM`の照合に基づき、三音源共通の`@r` Attack/Decay出力、PSG/SCCのキーオフ翌カウントからのRR、およびOPLLの同カウントハードウェアキーオフ後もRRへ移らずSRを継続する量子化系列を固定 |
| 0.14 | 2026-07-25 | `TEST_BOUNDARY.MGS/VGM`の照合に基づき、PSGキーオン時の初期Mode/Noise・基本周期・ゼロ時間命令・先頭音量の出力順、正数周波数変更による周期減算、およびランプ終端と後続音量の同一割り込み二重書き込みを固定 |
| 0.13 | 2026-07-25 | 実MGSC生成MGSと`TEST.VGM`の照合に基づき、`@e`先頭命令はキーオン翌割り込みではなくキーオンと同じ割り込み内で実行されるよう仕様を訂正 |
| 0.12 | 2026-07-24 | MGSDRV 9音モードのまま`y`でYM2413をリズムモード化する「9音リズムモード」、OPLL Ch.7～9と打楽器音量ニブルの対応、および`@15`が共有オリジナル音色を@15へ書き戻すため打楽器音量用途では使用禁止となる制約を追加 |
| 0.11 | 2026-07-24 | MGSDRV 3.20のPSG `s`・`m`・`v`およびキーオン経路解析に基づき、共有ハードウェアEG周期・形状、キーオン時のレジスタ13再始動、減衰8以上での再始動抑止、`v`による解除、`@e`/`@r`選択時の保持、および再キーオン初期化を追加 |
| 0.10 | 2026-07-24 | MGSDRV 3.20の音量出力経路解析に基づき、`@e`の加算型トラック音量合成、`@r`の8bit乗算型合成、共通のマスター／トラック追加減衰、およびPSG・SCC・OPLL別レジスタ値変換を追加 |
| 0.9 | 2026-07-24 | MGSDRV 3.20のゲート処理解析に基づき、PSG/SCCのAttack・Decay中キーオフをSustain到達まで予約する`@r` Release移行と、OPLLではハードウェアEGへキーオフして`@r` RRへ移行しない音源別挙動を追加 |
| 0.8 | 2026-07-24 | MGSDRV 3.20バイナリおよびMML/MGS/VGM対応サンプルの解析に基づき、全`@e`命令対応、キーオン翌割り込みからの実行、待ち終了カウント内の継続処理、整数剰余分配による音量ランプ、累積周波数変更、命令直列化順、`y`の直接書き込み、`@r`専用レート型編集と実行式を追加 |
| 0.7 | 2026-07-24 | SCCプリセットの標準マージを位相合わせ付き・音量正規化リニアマージとして確定し、1.5x倍音ブレンド、同じ処理基盤によるSCCモーフ中間波形生成、連続キーフレーム・往復・ループの位相整合を追加 |
| 0.6 | 2026-07-24 | SCCグラフと32数値欄の軸合わせ、直接数値入力、マウス描画、基本波形プリセット、倍音倍率、現在波形とのマージ、循環移動平均、任意名保存、およびOPLL・SCCのライブラリ名とMGSC音色番号の完全分離を追加 |
| 0.5 | 2026-07-24 | OPLL・SCC単音色編集時の即時発声、ドラッグ中の値変更ごとの再発声、最後に手動発声した音階の共有、音源別チェックボックスによるON / OFFを追加 |
| 0.4 | 2026-07-24 | OPLL・SCC単音色をソフトウェアエンベロープのない別ウィンドウで編集する方針、単音色ライブラリ区分、OPLL MOD / CARエンベロープのグラフ表示、ドラッグ・数値・縦バーによるAR / DR / SL / RR同期編集を追加 |
| 0.3 | 2026-07-24 | 1カウント約1/60秒固定、三音源エンベロープの同一時間軸表示、パッチイベントからキーオン・保持カウントを除外、音量`0`～`f`、全音色パラメーター編集、PCキーボード試聴、TL/FB個別ON/OFFを追加 |
| 0.2 | 2026-07-23 | OPLLパッチスライドをキーオン中の離散的な音色番号切り替えとして定義し、`y`レジスタオートメーションとの同時使用、イベント適用順、共有レジスタ競合、受け入れ条件を追加 |
| 0.1 | 2026-07-23 | 会話で確定した初期要求、採用音源コア、MGSC出力、ライブラリ、OPLLオートメーション、SCCモーフ仕様を固定 |
